<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_crawler" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.crawler</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="90"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="HLV Product Crawler" string="HLV Product Crawler" name="hlv_product_crawler">
                    <block title="Kết nối AI (OpenAI)" name="ai_settings">
                        <setting string="OpenAI API Key" help="API Key từ OpenAI để xác minh và phân tích dữ liệu.">
                            <field name="product_crawler_openai_api_key" password="True"/>
                        </setting>
                        <setting string="OpenAI Model" help="Model (ví dụ: gpt-4o-mini, gpt-3.5-turbo).">
                            <field name="product_crawler_openai_model"/>
                        </setting>
                    </block>

                    <block title="Cấu hình Tự động (Cron)" name="cron_settings">
                        <setting string="Tự động chạy ngầm" help="Bật tính năng tự động chạy ngầm (Scheduled Action).">
                            <field name="product_crawler_auto_crawl"/>
                        </setting>
                        <setting string="Batch Size" help="Số lượng sản phẩm xử lý mỗi lần chạy.">
                            <field name="product_crawler_batch_size"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_crawler_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'hlv_product_crawler'}</field>
    </record>

    <menuitem id="menu_crawler_config_settings" 
        name="Cấu hình Crawler" 
        parent="stock.menu_stock_config_settings" 
        action="action_crawler_config_settings" 
        sequence="100"/>
</odoo>
