<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="barcode_rule_pick_done_redirect" model="barcode.rule">
    <field name="name">Redirect Pick Done to Pack</field>
    <field name="type">python</field>
    <field name="model_id" ref="stock.model_stock_picking"/>
    <field name="domain">[("state", "=", "done")]</field>
    <field name="barcode_pattern">.*</field>
    <field name="sequence">0</field>
    <field name="python_code">
pack = env['stock.picking'].search([
    ('origin', '=', record.name),
    ('state', 'not in', ['done', 'cancel']),
    ('picking_type_id.code', '=', 'outgoing')
], limit=1)

if pack:
    action = {
        'type': 'ir.actions.client',
        'tag': 'reload',
        'params': {
            'barcode_picking_id': pack.id
        }
    }
else:
    action = {'warning': 'Không tìm thấy phiếu pack liên quan chưa done.'}
    </field>
  </record>
</odoo>
