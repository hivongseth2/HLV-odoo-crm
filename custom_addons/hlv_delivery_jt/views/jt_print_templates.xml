<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="jt_print_label_template" name="J&amp;T Print Label Template">
        <html>
            <head>
                <title>In Tem J&amp;T Express</title>
                <style>
                    body, html {
                        margin: 0;
                        padding: 0;
                        height: 100%;
                        width: 100%;
                        overflow: hidden;
                        background-color: #f0f0f0;
                    }
                    #print_frame {
                        width: 100%;
                        height: 100%;
                        border: none;
                    }
                    .print-notice {
                        position: fixed;
                        top: 20px;
                        left: 50%;
                        transform: translateX(-50%);
                        background: rgba(0,0,0,0.7);
                        color: white;
                        padding: 10px 20px;
                        border-radius: 5px;
                        font-family: sans-serif;
                        z-index: 1000;
                        pointer-events: none;
                    }
                </style>
                <script type="text/javascript">
                    function triggerPrint() {
                        var frame = document.getElementById('print_frame');
                        try {
                            frame.contentWindow.print();
                        } catch (e) {
                            // Fallback if cross-origin or other issues, though should be same origin here
                            window.print();
                        }
                    }
                    
                    window.onload = function() {
                        // Small delay to ensure PDF viewer is ready
                        setTimeout(triggerPrint, 1000);
                    };
                </script>
            </head>
            <body>
                <div class="print-notice">Đang chuẩn bị in... Vui lòng đợi trong giây lát.</div>
                <iframe id="print_frame" t-attf-src="/web/content/{{attachment_id}}"></iframe>
            </body>
        </html>
    </template>
</odoo>
