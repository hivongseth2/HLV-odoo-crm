<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="hlv_sale_delivery_planning.Dashboard">
        <div class="o_action o_view_controller hlv_delivery_planner_dashboard" style="height: 100%; display: flex; flex-direction: column;">
            <div class="o_content bg-view bg-light" style="flex: 1; overflow-y: auto;">
                <div class="container-fluid pt-4 pb-5">
                    <div class="d-flex justify-content-between align-items-center mb-4 pb-3 border-bottom">
                        <h2 class="text-primary m-0 fw-bold"><i class="fa fa-truck me-2"></i> BẢNG ĐIỀU PHỐI GIAO HÀNG</h2>
                        <button class="btn btn-primary shadow-sm" t-on-click="fetchData" t-att-disabled="state.isLoading">
                            <i class="fa fa-refresh me-1" t-att-class="{'fa-spin': state.isLoading}"></i> Làm mới
                        </button>
                    </div>

                    <div t-if="state.isLoading" class="text-center p-5 mt-5">
                        <div class="spinner-border text-primary" role="status" style="width: 3rem; height: 3rem;">
                            <span class="visually-hidden">Loading...</span>
                        </div>
                        <div class="mt-3 text-muted">Đang tải dữ liệu, vui lòng đợi...</div>
                    </div>

                    <div t-else="">
                        <!-- KPI Dashboard Thống Kê -->
                        <div class="row g-3 mb-4">
                            <div class="col-md-3">
                                <div class="card bg-primary text-white shadow-sm border-1 h-100" style="cursor: pointer;" t-on-click="() => this.setStockFilter('all')">
                                    <div class="card-body d-flex align-items-center justify-content-between p-3" title="Click để xem Tất cả (Bỏ lọc Kho)">
                                        <div>
                                            <h6 class="text-uppercase fw-bold mb-1 opacity-75 small">Tổng Đơn Đang Mở</h6>
                                            <h3 class="mb-0 fw-bold"><t t-esc="state.dashboardStats.total || 0"/></h3>
                                        </div>
                                        <div class="fa fa-shopping-cart fa-3x opacity-50"></div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="card bg-success text-white shadow-sm border-1 h-100" style="cursor: pointer;" t-on-click="() => this.setStockFilter('ready')">
                                    <div class="card-body d-flex align-items-center justify-content-between p-3" title="Click để xem Đủ hàng xuất">
                                        <div>
                                            <h6 class="text-uppercase fw-bold mb-1 opacity-75 small">Sẵn Sàng Xuất Đủ</h6>
                                            <h3 class="mb-0 fw-bold"><t t-esc="state.dashboardStats.ready || 0"/></h3>
                                        </div>
                                        <div class="fa fa-check-circle fa-3x opacity-50"></div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="card bg-warning text-dark shadow-sm border-1 h-100" style="cursor: pointer;" t-on-click="() => this.setStockFilter('partial_ready')">
                                    <div class="card-body d-flex align-items-center justify-content-between p-3" title="Click để xem Có hàng 1 phần">
                                        <div>
                                            <h6 class="text-uppercase fw-bold mb-1 opacity-75 small">Có Hàng 1 Phần</h6>
                                            <h3 class="mb-0 fw-bold"><t t-esc="state.dashboardStats.partial || 0"/></h3>
                                        </div>
                                        <div class="fa fa-exclamation-circle fa-3x opacity-50"></div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="card bg-danger text-white shadow-sm border-1 h-100" style="cursor: pointer;" t-on-click="() => this.setStockFilter('out_of_stock')">
                                    <div class="card-body d-flex align-items-center justify-content-between p-3" title="Click để xem Thiếu hàng">
                                        <div>
                                            <h6 class="text-uppercase fw-bold mb-1 opacity-75 small">Chưa Có Hàng / Thiếu</h6>
                                            <h3 class="mb-0 fw-bold"><t t-esc="state.dashboardStats.out_of_stock || 0"/></h3>
                                        </div>
                                        <div class="fa fa-times-circle fa-3x opacity-50"></div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!-- KPI Đóng Gói / Kiện Hàng -->
                        <div class="row g-3 mb-4">
                            <div class="col-md-3">
                                <div class="card bg-white text-dark shadow-sm border-0 h-100 kpi-card" style="cursor: pointer;" t-on-click="() => this.setPackingFilter('waiting_stock')" t-att-class="state.filterPackingStatus === 'waiting_stock' ? 'ring-2 ring-danger' : ''">
                                    <div class="card-body d-flex align-items-center p-3" title="Chưa đủ hàng để đóng kiện">
                                        <div class="rounded-circle bg-danger bg-opacity-10 p-3 me-3 text-danger"><i class="fa fa-clock-o fa-fw fa-2x"></i></div>
                                        <div>
                                            <h6 class="text-uppercase fw-bold text-muted mb-1 small opacity-75">Chờ Cấp Hàng</h6>
                                            <h3 class="mb-0 fw-bold"><t t-esc="state.dashboardStats.packing_waiting || 0"/></h3>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="card bg-white text-dark shadow-sm border-0 h-100 kpi-card" style="cursor: pointer;" t-on-click="() => this.setPackingFilter('unpacked')" t-att-class="state.filterPackingStatus === 'unpacked' ? 'ring-2 ring-warning' : ''">
                                    <div class="card-body d-flex align-items-center p-3" title="Có Hàng Nằm Chờ Đóng Gói">
                                        <div class="rounded-circle bg-warning bg-opacity-10 p-3 me-3 text-warning"><i class="fa fa-cubes fa-fw fa-2x"></i></div>
                                        <div>
                                            <h6 class="text-uppercase fw-bold text-muted mb-1 small opacity-75">Hàng Chờ Đóng</h6>
                                            <h3 class="mb-0 fw-bold"><t t-esc="state.dashboardStats.packing_unpacked || 0"/></h3>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="card bg-white text-dark shadow-sm border-0 h-100 kpi-card" style="cursor: pointer;" t-on-click="() => this.setPackingFilter('partial_packed')" t-att-class="state.filterPackingStatus === 'partial_packed' ? 'ring-2 ring-info' : ''">
                                    <div class="card-body d-flex align-items-center p-3" title="Đã Đóng 1 Phần Đơn">
                                        <div class="rounded-circle bg-info bg-opacity-10 p-3 me-3 text-info"><i class="fa fa-cube fa-fw fa-2x"></i></div>
                                        <div>
                                            <h6 class="text-uppercase fw-bold text-muted mb-1 small opacity-75">Đóng 1 Phần</h6>
                                            <h3 class="mb-0 fw-bold"><t t-esc="state.dashboardStats.packing_partial || 0"/></h3>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="card bg-white text-dark shadow-sm border-0 h-100 kpi-card" style="cursor: pointer;" t-on-click="() => this.setPackingFilter('fully_packed')" t-att-class="state.filterPackingStatus === 'fully_packed' ? 'ring-2 ring-success' : ''">
                                    <div class="card-body d-flex align-items-center p-3" title="Đã Đóng Đủ Kiện Sẵn Sàng Giao">
                                        <div class="rounded-circle bg-success bg-opacity-10 p-3 me-3 text-success"><i class="fa fa-check-square-o fa-fw fa-2x"></i></div>
                                        <div>
                                            <h6 class="text-uppercase fw-bold text-muted mb-1 small opacity-75">Đã Đủ Kiện</h6>
                                            <h3 class="mb-0 fw-bold"><t t-esc="state.dashboardStats.packing_fully || 0"/></h3>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <!-- Active Filters Bar -->
                    <div t-if="this.hasActiveFilters" class="mb-4 d-flex flex-wrap align-items-center gap-2 px-3 py-2 bg-light rounded border border-info border-opacity-25 w-100 shadow-sm mx-auto" style="max-width: 98%;">
                        <span class="text-secondary small fw-bold me-2"><i class="fa fa-filter me-1"></i> Bộ lọc đang chọn:</span>
                        
                        <span t-if="state.searchQuery" class="badge text-bg-info d-flex align-items-center gap-2 p-2 rounded-pill">
                            Tìm: "<t t-esc="state.searchQuery"/>"
                            <i class="fa fa-times cursor-pointer ms-1" t-on-click="() => { state.searchQuery = ''; this.fetchData(); }"></i>
                        </span>

                        <span t-if="state.filterWarehouseId !== 'all'" class="badge text-bg-info d-flex align-items-center gap-2 p-2 rounded-pill">
                            Kho: <t t-esc="state.warehouses.find(w => w.id == state.filterWarehouseId)?.name"/>
                            <i class="fa fa-times cursor-pointer ms-1" t-on-click="() => { state.filterWarehouseId = 'all'; this.fetchData(); }"></i>
                        </span>

                        <span t-if="state.filterPackingStatus !== 'all'" class="badge text-bg-info d-flex align-items-center gap-2 p-2 rounded-pill">
                            Đóng gói: <t t-esc="this.translatePackingStatus(state.filterPackingStatus)"/>
                            <i class="fa fa-times cursor-pointer ms-1" t-on-click="() => { state.filterPackingStatus = 'all'; this.fetchData(); }"></i>
                        </span>

                        <span t-if="state.filterDeliveryStatus !== 'all'" class="badge text-bg-info d-flex align-items-center gap-2 p-2 rounded-pill">
                            Giao hàng: <t t-esc="this.translateDeliveryStatus(state.filterDeliveryStatus)"/>
                            <i class="fa fa-times cursor-pointer ms-1" t-on-click="() => { state.filterDeliveryStatus = 'all'; this.fetchData(); }"></i>
                        </span>

                        <span t-if="state.filterStockStatus !== 'all'" class="badge text-bg-info d-flex align-items-center gap-2 p-2 rounded-pill">
                            Tồn kho: <t t-esc="this.translateStockStatus(state.filterStockStatus)"/>
                            <i class="fa fa-times cursor-pointer ms-1" t-on-click="() => { state.filterStockStatus = 'all'; this.fetchData(); }"></i>
                        </span>
                        
                        <button class="btn btn-sm btn-outline-danger border-0 fw-bold ms-auto" t-on-click="resetFilters">
                            <i class="fa fa-trash-o me-1"></i> Xóa tất cả bộ lọc
                        </button>
                    </div>

                    <!-- Thanh Công Cụ (Kho, Tìm kiếm, Lọc trạng thái, Hẹn giao) -->
                        <div class="row g-3 mb-4 bg-white p-3 rounded shadow-sm border align-items-end">
                            <!-- Ô tìm kiếm -->
                            <div class="col-md-3">
                                <label class="form-label small fw-bold text-muted mb-1">Tìm Kiếm</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light px-2 cursor-pointer" t-on-click="onFilterChange"><i class="fa fa-search text-primary"></i></span>
                                    <input type="text" class="form-control px-2 border-1 fw-bold" placeholder="Tìm tên/mã Khách, SO..." t-model="state.searchQuery" t-on-keyup="onSearchKeyup"/>
                                </div>
                            </div>
                            
                            <!-- Lọc theo kho -->
                            <div class="col-md-2">
                                <label class="form-label small fw-bold text-muted mb-1">Kho Cung Cấp</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light"><i class="fa fa-building text-muted"></i></span>
                                    <select class="form-select" t-model="state.filterWarehouseId" t-on-change="onFilterChange">
                                        <option value="all">Tất cả</option>
                                        <t t-foreach="state.warehouses" t-as="wh" t-key="wh.id">
                                            <option t-att-value="wh.id"><t t-esc="wh.name"/></option>
                                        </t>
                                    </select>
                                </div>
                            </div>

                            <!-- Tiến độ Giao hàng -->
                            <div class="col-md-2">
                                <label class="form-label small fw-bold text-muted mb-1">Tiến Độ Giao</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light px-2"><i class="fa fa-truck text-primary"></i></span>
                                    <select class="form-select px-1 fw-bold" t-model="state.filterDeliveryStatus" t-on-change="onFilterChange">
                                        <option value="all">Tất cả</option>
                                        <option value="pending_partial">Chưa giao &amp; Giao 1 phần</option>
                                        <option value="unshipped">Chưa giao</option>
                                        <option value="partial">Giao 1 phần</option>
                                        <option value="full">Hoàn thành</option>
                                    </select>
                                </div>
                            </div>
                            
                            <!-- Tình Trạng Kho -->
                            <div class="col-md-2">
                                <label class="form-label small fw-bold text-muted mb-1">Tình Trạng Kho</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light px-2"><i class="fa fa-cubes text-warning"></i></span>
                                    <select class="form-select px-1 fw-bold" t-model="state.filterStockStatus" t-on-change="onFilterChange">
                                        <option value="all">Tất cả</option>
                                        <option value="out_of_stock">Không có hàng</option>
                                        <option value="partial_ready">Có hàng 1 phần</option>
                                        <option value="ready">Đủ hàng xuất</option>
                                    </select>
                                </div>
                            </div>

                            <!-- Lọc theo ngày hẹn giao SO -->
                            <div class="col-md-3">
                                <label class="form-label small fw-bold text-muted mb-1">Ngày Hẹn Giao (SO)</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light px-2" title="Từ Ngày"><i class="fa fa-calendar text-primary"></i></span>
                                    <input type="date" class="form-control px-1" title="Từ Ngày" t-model="state.filterDateFrom" t-on-change="onFilterChange" />
                                    <span class="input-group-text bg-light px-2 border-start-0 border-end-0">~</span>
                                    <input type="date" class="form-control px-1" title="Đến Ngày" t-model="state.filterDateTo" t-on-change="onFilterChange" />
                                </div>
                            </div>

                            <!-- Lọc theo ngày hẹn nhận PO -->
                            <div class="col-md-3">
                                <label class="form-label small fw-bold text-muted mb-1">Ngày Hẹn Nhận (PO)</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light px-2" title="Từ Ngày"><i class="fa fa-calendar-o text-info"></i></span>
                                    <input type="date" class="form-control px-1" title="Từ Ngày" t-model="state.filterPODateFrom" t-on-change="onFilterChange" />
                                    <span class="input-group-text bg-light px-2 border-start-0 border-end-0">~</span>
                                    <input type="date" class="form-control px-1" title="Đến Ngày" t-model="state.filterPODateTo" t-on-change="onFilterChange" />
                                </div>
                            </div>

                            <!-- Trạng Thái Lệnh Mua (PO) -->
                            <div class="col-md-3 mt-md-0 mt-3">
                                <label class="form-label small fw-bold text-muted mb-1">Trạng Thái (PO)</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-light px-2"><i class="fa fa-file-text-o text-info"></i></span>
                                    <select class="form-select px-1 fw-bold" t-model="state.filterPOStatus" t-on-change="onFilterChange">
                                        <option value="all">Tất cả</option>
                                        <option value="pending">Chưa nhận hàng</option>
                                        <option value="partial">Nhận 1 phần</option>
                                        <option value="full">Đã nhận đủ</option>
                                    </select>
                                </div>
                            </div>
                        </div>

                        <div t-if="!state.saleOrders.length" class="alert alert-info shadow-sm d-flex align-items-center p-4" role="alert">
                            <i class="fa fa-info-circle fa-2x me-3"></i> 
                            <div>
                                <h5 class="mb-1">Không có dữ liệu!</h5>
                                <div class="mb-0">Không tìm thấy đơn hàng nào phù hợp với bộ lọc hiện tại.</div>
                            </div>
                        </div>

                        <!-- Danh sách SO -->
                        <div t-else="" class="row g-4 mt-2">
                            <t t-foreach="this.paginatedOrders" t-as="so" t-key="so.id">
                            <div class="col-12 col-xl-4 col-lg-6">
                                <div class="card h-100 border-1" t-att-class="this.getSOCardColorClass(so)">
                                    <div class="card-header d-flex justify-content-between align-items-center py-3 border-bottom-1">
                                        <div>
                                            <div class="d-flex align-items-center gap-2 mb-2 flex-wrap">
                                                <span class="badge" t-att-class="this.getDeliveryStatusBadgeClass(so.real_delivery_status)"><t t-esc="this.translateDeliveryStatus(so.real_delivery_status)"/></span>
                                                <span class="badge" t-att-class="this.getStockStatusBadgeClass(so.stock_status)" t-if="so.real_delivery_status != 'full'"><t t-esc="this.translateStockStatus(so.stock_status)"/></span>
                                                <span class="badge" t-att-class="this.getPackingStatusBadgeClass(so.packing_status)" t-if="so.real_delivery_status != 'full' and so.packing_status != 'unknown'"><i class="fa fa-cube me-1"></i><t t-esc="this.translatePackingStatus(so.packing_status)"/></span>
                                            </div>
                                            <h5 class="card-title mb-1 text-primary cursor-pointer o_form_uri" t-on-click="() => this.openSaleOrder(so.id)">
                                                <i class="fa fa-shopping-cart text-muted me-1"></i> <t t-esc="so.name"/>
                                            </h5>
                                            <div class="text-muted small fw-bold">
                                                <i class="fa fa-user text-info me-1"></i> <t t-esc="so.partner_id ? so.partner_id[1] : 'Unknown'"/>
                                            </div>
                                        </div>
                                        <div class="text-end">
                                            <button class="btn btn-sm btn-outline-secondary mb-1" t-on-click="() => this.openOverviewDrawer(so)">
                                                <i class="fa fa-eye"></i> Tổng Quan
                                            </button>
                                            <div class="small fw-bold text-nowrap mt-1" t-if="so.commitment_date">
                                                <i class="fa fa-calendar-check-o text-danger me-1"></i>
                                                Giao: <t t-esc="so.commitment_date.substring(0, 10)"/>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="card-body pt-2 pb-0">
                                        <div class="d-flex justify-content-between align-items-center mb-2 px-1">
                                            <h6 class="text-uppercase text-secondary small fw-bold mb-0"><i class="fa fa-list me-1"></i> Đơn Mua Liên Quan</h6>
                                        </div>
                                        
                                        <div t-if="!so.pos.length" class="alert alert-warning py-2 small mb-3 border-1">
                                            <i class="fa fa-exclamation-triangle me-1"></i> Không có đơn mua hàng
                                        </div>
                                        <div t-else="" class="list-group mb-3 gap-2 border-1">
                                            <t t-foreach="so.pos" t-as="po" t-key="po.id">
                                                <button class="list-group-item list-group-item-action d-flex justify-content-between border rounded shadow-sm p-2 bg-white" 
                                                        t-on-click="() => this.openPurchaseOrder(po.id)">
                                                    <div class="d-flex flex-column align-items-start">
                                                        <span class="fw-bold text-dark"><i class="fa fa-file-text-o text-muted me-1"></i> <t t-esc="po.name"/></span>
                                                        <div class="small mt-1" t-if="po.date_planned" t-att-class="this.getDatesComparisonClass(so.commitment_date, po.date_planned)">
                                                            <i class="fa fa-clock-o me-1"></i> Dự kiến về: <t t-esc="po.date_planned.substring(0, 10)"/>
                                                            <i t-if="so.commitment_date and po.date_planned > so.commitment_date" class="fa fa-warning ms-1 text-danger" title="Hàng về trễ so với ngày dự giao SO!"></i>
                                                        </div>
                                                    </div>
                                                    <div class="d-flex align-items-center">
                                                        <span class="badge" t-att-class="this.getPOStatusBadgeClass(po.state, po.receipt_status)">
                                                            <t t-esc="this.translatePOStatus(po.receipt_status || po.state)"/>
                                                        </span>
                                                    </div>
                                                </button>
                                            </t>
                                        </div>
                                        
                                        <!-- Danh Sách Kiện Hàng Đã Đóng (Packages) dạng Hộp -->
                                        <div t-if="so.packages and so.packages.length > 0" class="mb-3 px-1">
                                            <h6 class="text-uppercase text-secondary small fw-bold mb-3"><i class="fa fa-cubes me-1"></i> Thông Tin Kiện Hàng (<t t-esc="so.total_packages_count"/>)</h6>
                                            <div class="package-groups">
                                                <t t-foreach="so.packages" t-as="group" t-key="group.picking_id">
                                                    <div class="picking-package-group mb-3 pb-3 border-bottom">
                                                        <div class="d-flex align-items-center justify-content-between mb-2">
                                                            <div class="d-flex align-items-center flex-grow-1">
                                                                <span class="badge bg-secondary bg-opacity-10 text-primary border border-secondary border-opacity-25 py-1 px-2 small">
                                                                    <i class="fa fa-file-text-o me-1"></i> <t t-esc="group.picking_name"/>
                                                                </span>
                                                                <span t-attf-class="ms-2 badge  text-bg-info  rounded-pill #{this.getPickingStateBadgeClass(group.picking_state)}" style="font-size: 0.65rem;">
                                                                    <t t-esc="this.translatePickingState(group.picking_state)"/>
                                                                </span>
                                                                <hr class="flex-grow-1 ms-2 my-0 opacity-25"/>
                                                            </div>
                                                        </div>
                                                        <div class="d-flex flex-wrap gap-2 package-grid">
                                                            <t t-foreach="group.packages" t-as="pack" t-key="pack.id">
                                                                <div class="package-box-container position-relative" t-att-title="'Bấm để xem chi tiết: ' + pack.name" t-on-click="() => this.openPackageDetails(pack)">
                                                                    <div class="package-box d-flex flex-column align-items-center justify-content-center border rounded shadow-sm bg-white p-1" style="min-width: 85px;">
                                                                        <i class="fa fa-archive fa-2x text-primary mb-1"></i>
                                                                        <span class="small text-truncate w-100 px-1 text-center" style="font-size: 0.60rem;"><t t-esc="pack.name"/></span>
                                                                    </div>
                                                                </div>
                                                            </t>
                                                        </div>
                                                    </div>
                                                </t>
                                            </div>
                                        </div>
                                        
                                        <!-- LUỒNG XỬ LÝ KHO NGANG 1 CHIỀU THEO TỪNG PATH -->
                                        <div t-if="so.flows and so.flows.length > 0">
                                            <h6 class="text-uppercase text-secondary small fw-bold mb-2 mt-2 px-1"><i class="fa fa-sitemap me-1"></i> Luồng Xử Lý Kho</h6>
                                            
                                            <!-- Lặp qua từng tuyến đường độc lập (Path) -->
                                            <t t-foreach="so.flows" t-as="flow" t-key="flow.id">
                                                <div class="d-flex align-items-center overflow-auto pb-3 pt-1 gap-2 px-1 border-1" style="white-space: nowrap;">
                                                    
                                                    <t t-if="flow.is_return">
                                                        <!-- KHÁCH HÀNG TẠI ĐẦU SƠ ĐỒ TRẢ -->
                                                        <div class="d-flex flex-column justify-content-start align-items-center text-danger px-2 border border-danger rounded p-2 bg-light">
                                                            <i class="fa fa-user fa-2x mb-1"></i>
                                                            <small class="fw-bold text-truncate" style="max-width: 100px;" t-att-title="so.partner_id ? so.partner_id[1] : 'Khách hàng'"><t t-esc="so.partner_id ? so.partner_id[1] : 'Khách hàng'"/></small>
                                                        </div>
                                                        <div class="text-danger mx-1"><i class="fa fa-long-arrow-right fa-lg"></i></div>

                                                        <!-- Từng Node trong Path Trả -->
                                                        <t t-foreach="flow.nodes" t-as="picking" t-key="picking.id">
                                                            <div class="card shadow-sm border border-2 cursor-pointer text-center p-2 rounded picking-node" 
                                                                 style="min-width: 140px; transition: all 0.3s ease;"
                                                                 t-attf-class="{{ picking.state == 'done' ? 'bg-light opacity-75 border-secondary' : 'bg-white border-danger' }} {{ picking.return_of ? 'linked-return-' + picking.return_of.split('/').join('-') : '' }}"
                                                                 t-att-data-picking-name="picking.name.split('/').join('-')"
                                                                 t-on-mouseenter="() => this.onPickingHover(picking.name)"
                                                                 t-on-mouseleave="() => this.onPickingLeave()"
                                                                 t-on-click="() => this.openPicking(picking.id)">
                                                                <!-- Status checkmarks and link indicator -->
                                                                <span t-if="picking.link_color" class="position-absolute top-0 start-0 translate-middle badge rounded-pill border border-light shadow-sm" t-attf-class="bg-{{picking.link_color}}" style="font-size: 0.65rem; z-index: 5;">
                                                                    <t t-if="picking.return_of or picking.backorder_of">
                                                                        <i class="fa fa-link"></i> Nối <t t-if="picking.parent_seq">[<t t-esc="picking.parent_seq"/>]</t>
                                                                    </t>
                                                                    <t t-else="">
                                                                        <i class="fa fa-link"></i> Gốc
                                                                    </t>
                                                                </span>
                                                                
                                                                <div class="small fw-bold text-truncate mb-1" 
                                                                     t-att-class="picking.state == 'done' ? 'text-secondary' : 'text-danger'">
                                                                    <span t-if="picking.global_seq > 0" class="badge bg-secondary me-1" title="Thứ tự thời gian"><t t-esc="picking.global_seq"/></span><t t-esc="picking.type_name"/>
                                                                </div>
                                                                <span class="badge w-100 mb-1" t-att-class="this.getPickingStatusBadgeClass(picking.state)">
                                                                    <t t-esc="this.translatePickingStatus(picking.state)"/>
                                                                </span>
                                                                <!-- Status checkmark removed from picking card -->
                                                                <div class="small fw-bold" t-att-class="picking.state == 'done' ? 'text-muted' : 'text-dark'"><t t-esc="picking.name"/></div>
                                                                
                                                                <!-- Backorder / Return label -->
                                                                <div t-if="picking.return_of" class="text-danger mt-1 text-truncate" style="font-size: 0.70rem;" t-att-title="'Lấy lại từ: ' + picking.return_of"><i class="fa fa-undo"></i> Từ: <t t-esc="picking.return_of"/></div>
                                                                <div t-if="picking.backorder_of" class="text-muted mt-1 text-truncate" style="font-size: 0.70rem;" t-att-title="'Cắt từ: ' + picking.backorder_of"><i class="fa fa-files-o"></i> Cắt từ: <t t-esc="picking.backorder_of"/></div>

                                                                <!-- Video Badge -->
                                                                <div t-if="picking.videos and picking.videos.length > 0" class="mt-1 d-flex flex-wrap justify-content-center gap-1">
                                                                    <t t-foreach="picking.videos" t-as="video" t-key="video.id">
                                                                        <span class="badge bg-danger rounded-pill cursor-pointer" t-on-click.stop="() => this.openVideo(video.url)" title="Xem Camera Đóng Gói / Trả hàng">
                                                                            <i class="fa fa-play-circle"></i> Cam
                                                                        </span>
                                                                    </t>
                                                                </div>
                                                            </div>
                                                            <div class="mx-1" t-att-class="picking.state == 'cancel' ? 'text-secondary' : 'text-danger'">
                                                                <i class="fa fa-long-arrow-right fa-lg"></i>
                                                            </div>
                                                        </t>

                                                        <!-- KHO CUNG CẤP TẠI CUỐI SƠ ĐỒ TRẢ -->
                                                        <div class="d-flex position-relative flex-column justify-content-start align-items-center text-muted px-2 border rounded p-2 bg-light">
                                                            <span t-if="flow.nodes.length &gt; 0 and flow.nodes[flow.nodes.length - 1].state == 'done'" class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-success">
                                                                <i class="fa fa-check"></i>
                                                            </span>
                                                            <i class="fa fa-building fa-2x mb-1"></i>
                                                            <small class="fw-bold text-truncate" style="max-width: 100px;" t-att-title="'Kho ' + (so.warehouse_id ? so.warehouse_id[1] : '')">Kho</small>
                                                        </div>
                                                    </t>
                                                    <t t-else="">
                                                        <!-- KHO CUNG CẤP TẠI ĐẦU SƠ ĐỒ XUẤT -->
                                                        <div class="d-flex flex-column justify-content-start align-items-center text-muted px-2 border rounded p-2 bg-light">
                                                            <i class="fa fa-building fa-2x mb-1"></i>
                                                            <small class="fw-bold text-truncate" style="max-width: 100px;" t-att-title="'Kho ' + (so.warehouse_id ? so.warehouse_id[1] : '')">Kho</small>
                                                        </div>
                                                        <div class="text-muted mx-1"><i class="fa fa-long-arrow-right fa-lg"></i></div>

                                                        <!-- Từng Node trong Path Xuất -->
                                                        <t t-foreach="flow.nodes" t-as="picking" t-key="picking.id">
                                                            <div class="card shadow-sm border border-2 cursor-pointer text-center p-2 rounded picking-node" 
                                                                 style="min-width: 140px; transition: all 0.3s ease;"
                                                                 t-attf-class="{{ picking.state == 'done' ? 'bg-light opacity-75 border-secondary' : 'bg-white border-primary' }} {{ 'original-picking-' + picking.name.split('/').join('-') }}"
                                                                 t-att-data-picking-name="picking.name.split('/').join('-')"
                                                                 t-on-mouseenter="() => this.onPickingHover(picking.name)"
                                                                 t-on-mouseleave="() => this.onPickingLeave()"
                                                                 t-on-click="() => this.openPicking(picking.id)">
                                                                <!-- Status checkmarks and link indicator -->
                                                                <span t-if="picking.link_color" class="position-absolute top-0 start-0 translate-middle badge rounded-pill border border-light shadow-sm" t-attf-class="bg-{{picking.link_color}}" style="font-size: 0.65rem; z-index: 5;">
                                                                    <t t-if="picking.return_of or picking.backorder_of">
                                                                        <i class="fa fa-link"></i>  <t t-if="picking.parent_seq">[<t t-esc="picking.parent_seq"/>]</t>
                                                                    </t>
                                                                    <t t-else="">
                                                                        <i class="fa fa-link"></i> Trả
                                                                    </t>
                                                                </span>
                                                                
                                                                <div class="small fw-bold text-truncate mb-1" 
                                                                     t-att-class="{
                                                                         'text-secondary': picking.state == 'done',
                                                                         'text-primary': picking.state != 'done'
                                                                     }">
                                                                    <span t-if="picking.global_seq > 0" class="badge bg-info text-white me-1" title="Thứ tự thời gian"><t t-esc="picking.global_seq"/></span><t t-esc="picking.type_name"/>
                                                                </div>
                                                                <span class="badge w-100 mb-1" t-att-class="this.getPickingStatusBadgeClass(picking.state)">
                                                                    <t t-esc="this.translatePickingStatus(picking.state)"/>
                                                                </span>
                                                                <!-- Status checkmark removed from picking card -->
                                                                <div class="small fw-bold" t-att-class="picking.state == 'done' ? 'text-muted' : 'text-dark'"><t t-esc="picking.name"/></div>
                                                                
                                                                <!-- Backorder / Return label -->
                                                                <div t-if="picking.backorder_of" class="text-muted mt-1 text-truncate" style="font-size: 0.70rem;" t-att-title="'Cắt từ: ' + picking.backorder_of"><i class="fa fa-files-o"></i> Cắt từ: <t t-esc="picking.backorder_of"/></div>

                                                                <!-- Video Badge -->
                                                                <div t-if="picking.videos and picking.videos.length > 0" class="mt-1 d-flex flex-wrap justify-content-center gap-1">
                                                                    <t t-foreach="picking.videos" t-as="video" t-key="video.id">
                                                                        <span class="badge bg-danger rounded-pill cursor-pointer" t-on-click.stop="() => this.openVideo(video.url)" title="Xem Camera Đóng Gói">
                                                                            <i class="fa fa-play-circle"></i> Cam
                                                                        </span>
                                                                    </t>
                                                                </div>
                                                            </div>
                                                            <div class="mx-1" t-att-class="picking.state == 'cancel' ? 'text-secondary' : 'text-muted'">
                                                                <i class="fa fa-long-arrow-right fa-lg"></i>
                                                            </div>
                                                        </t>

                                                        <!-- KHÁCH HÀNG TẠI CUỐI SƠ ĐỒ XUẤT -->
                                                        <div class="d-flex position-relative flex-column justify-content-start align-items-center text-primary px-2 border border-primary rounded p-2 bg-light">
                                                            <span t-if="flow.nodes.length &gt; 0 and flow.nodes[flow.nodes.length - 1].state == 'done'" class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-success">
                                                                <i class="fa fa-check"></i>
                                                            </span>
                                                            <span t-if="flow.nodes.length &gt; 0 and flow.nodes[flow.nodes.length - 1].state == 'cancel'" class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                                                <i class="fa fa-times"></i>
                                                            </span>
                                                            <i class="fa fa-user fa-2x mb-1"></i>
                                                            <small class="fw-bold text-truncate" style="max-width: 100px;" t-att-title="so.partner_id ? so.partner_id[1] : 'Khách hàng'"><t t-esc="so.partner_id ? so.partner_id[1] : 'Khách hàng'"/></small>
                                                        </div>
                                                    </t>
                                                </div>
                                            </t>
                                        </div>

                                        <h6 class="text-uppercase text-secondary small fw-bold mb-2 mt-2 px-1"><i class="fa fa-cubes me-1"></i> Sản Phẩm Đang Chờ</h6>
                                        <div class="table-responsive bg-white rounded shadow-sm border mb-3">
                                            <table class="table table-sm table-borderless table-hover mb-0 small">
                                                <thead class="table-light border-bottom">
                                                    <tr>
                                                        <th class="ps-3 text-muted">Sản phẩm</th>
                                                        <th class="text-end pe-3 text-muted" width="60">SL</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <t t-if="so.lines and so.lines.length > 0">
                                                        <t t-foreach="so.lines" t-as="line" t-key="line.id">
                                                            <tr t-if="line.product_uom_qty > line.qty_delivered">
                                                                <td class="ps-3 text-truncate" style="max-width: 200px;" t-att-title="line.product_id ? line.product_id[1] : 'Unknown'">
                                                                    <t t-esc="line.product_id ? line.product_id[1] : 'Unknown'"/>
                                                                </td>
                                                                 <td class="text-end pe-3 fw-bold">
                                                                    <div t-att-class="line.qty_available &gt;= (line.product_uom_qty - line.qty_delivered) ? 'text-success' : 'text-danger'">
                                                                        <t t-esc="this.formatQty(line.product_uom_qty - line.qty_delivered)"/>
                                                                        <i t-if="line.qty_available &gt;= (line.product_uom_qty - line.qty_delivered)" class="fa fa-check ms-1" title="Kho đã có đủ"></i>
                                                                    </div>
                                                                    <div t-if="line.qty_packed > 0" class="mt-1">
                                                                        <span class="badge rounded-pill bg-info text-dark" style="font-size: 0.65rem;" t-att-title="'Đã đóng gói: ' + line.qty_packed">
                                                                            <i class="fa fa-cube me-1"></i>Đóng: <t t-esc="this.formatQty(line.qty_packed)"/>
                                                                        </span>
                                                                    </div>
                                                                    <div t-else="" class="mt-1">
                                                                        <span class="badge rounded-pill bg-light text-muted border" style="font-size: 0.65rem;">
                                                                            <i class="fa fa-cube me-1 opacity-50"></i>Chưa đóng
                                                                        </span>
                                                                    </div>
                                                                </td>
                                                            </tr>
                                                        </t>
                                                    </t>
                                                    
                                                    <t t-if="so.real_delivery_status == 'full'">
                                                        <tr>
                                                            <td colspan="2" class="text-center text-success py-2 fw-bold"><i class="fa fa-star me-1"></i> Đã giao xong toàn bộ</td>
                                                        </tr>
                                                    </t>
                                                    <t t-elif="so.stock_status == 'ready'">
                                                        <tr>
                                                            <td colspan="2" class="text-center text-primary py-2 fw-bold"><i class="fa fa-check-circle me-1"></i> Sẵn sàng xuất kho đủ hàng</td>
                                                        </tr>
                                                    </t>
                                                    <t t-elif="so.stock_status == 'partial_ready'">
                                                        <tr>
                                                            <td colspan="2" class="text-center text-warning py-2 fw-bold"><i class="fa fa-exclamation-circle me-1"></i> Đã có hàng 1 phần</td>
                                                        </tr>
                                                    </t>
                                                    <t t-elif="so.stock_status == 'out_of_stock'">
                                                        <tr>
                                                            <td colspan="2" class="text-center text-danger py-2 fw-bold"><i class="fa fa-times-circle me-1"></i> Hết hàng</td>
                                                        </tr>
                                                    </t>
                                                </tbody>
                                            </table>
                                        </div>

                                    </div>
                                    <div class="card-footer border-top d-flex justify-content-between align-items-center p-3 text-bg-light">
                                        <div class="text-muted small"><i class="fa fa-money me-1"></i> Tổng giá trị đơn:</div>
                                        <div class="fw-bold text-primary fs-5">
                                            <t t-esc="this.formatCurrency(so.amount_total)"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </div>

                    <!-- Phân Trang (Sticky) -->
                    <div t-if="this.totalPages > 1" class="position-sticky bottom-0 w-100 z-3 bg-light pt-2 pb-3 mb-0 border-top mt-4" style="margin-left: -12px; padding-left: 12px; margin-right: -12px; padding-right: 12px;">
                        <div class="d-flex justify-content-between align-items-center bg-white p-3 rounded shadow-lg border">
                            <div class="text-muted small">
                                Hiển thị <span class="fw-bold"><t t-esc="(state.currentPage - 1) * state.itemsPerPage + 1"/></span> - 
                                <span class="fw-bold"><t t-esc="Math.min(state.currentPage * state.itemsPerPage, state.totalCount)"/></span> 
                                trong số <span class="fw-bold text-dark"><t t-esc="state.totalCount"/></span> đơn hàng
                            </div>
                            <nav aria-label="Page navigation">
                                <ul class="pagination mb-0">
                                    <li class="page-item" t-att-class="{'disabled': state.currentPage === 1}">
                                        <a class="page-link cursor-pointer" t-on-click="prevPage" aria-label="Previous">
                                            <span aria-hidden="true"><i class="fa fa-chevron-left"></i> Trang trước</span>
                                        </a>
                                    </li>
                                    <li class="page-item active"><a class="page-link" href="#"><t t-esc="state.currentPage"/> / <t t-esc="this.totalPages"/></a></li>
                                    <li class="page-item" t-att-class="{'disabled': state.currentPage === this.totalPages}">
                                        <a class="page-link cursor-pointer" t-on-click="nextPage" aria-label="Next">
                                            <span aria-hidden="true">Trang sau <i class="fa fa-chevron-right"></i></span>
                                        </a>
                                    </li>
                                </ul>
                            </nav>
                        </div>
                    </div>

                    </div> <!-- End of t-else main block -->
                </div>
            </div> <!-- End of o_content -->
            
            <!-- Side Drawer (Offcanvas) -->
            <div t-if="state.isDrawerOpen" class="offcanvas offcanvas-end show shadow-lg" tabindex="-1" style="visibility: visible; width: 680px; z-index: 1050; position: fixed; top: 0; bottom: 0; right: 0; background: white;">
                <div class="offcanvas-header bg-primary text-white p-3">
                    <h5 class="offcanvas-title m-0"><i class="fa fa-list-alt me-2"></i> Tổng Quan Đơn: <t t-esc="state.selectedOrder.name"/></h5>
                    <button type="button" class="btn-close btn-close-white ms-auto" aria-label="Close" t-on-click="closeOverviewDrawer"></button>
                </div>
                <div class="offcanvas-body p-4 text-start bg-light overflow-auto" style="height: calc(100vh - 150px);">
                    <div class="row bg-white p-3 rounded shadow-sm border mb-4 mx-0">
                        <div class="col-md-6 mb-2">
                            <h6 class="text-muted text-uppercase small mb-2">Khách Hàng</h6>
                            <div class="fs-6 fw-bold"><i class="fa fa-user me-2 text-primary"></i> <t t-esc="state.selectedOrder.partner_id ? state.selectedOrder.partner_id[1] : 'Unknown'"/></div>
                        </div>
                        <div class="col-md-6 mb-2">
                            <h6 class="text-muted text-uppercase small mb-2">Kho Cung Cấp</h6>
                            <div class="fs-6 fw-bold"><i class="fa fa-building me-2 text-info"></i> <t t-esc="state.selectedOrder.warehouse_id ? state.selectedOrder.warehouse_id[1] : 'Chưa định thông tin'"/></div>
                        </div>
                    </div>
                    
                    <h6 class="text-muted text-uppercase small mb-3"><i class="fa fa-cubes me-1"></i> Tình trạng Chi Tiết Sản Phẩm</h6>
                    <table class="table table-bordered table-sm table-hover text-center align-middle bg-white shadow-sm mb-4">
                        <thead class="table-light">
                            <tr>
                                <th class="text-start">Sản phẩm</th>
                                 <th width="70">Chốt Bán</th>
                                 <th width="70">Đóng Gói</th>
                                 <th width="70">Kho Hiện Có</th>
                                 <th width="70">Đã Giao</th>
                                 <th width="70">Còn Bị Thiếu</th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="state.selectedOrder.lines" t-as="line" t-key="line.id">
                                <tr t-att-class="line.product_uom_qty > line.qty_delivered ? 'table-warning' : 'table-success'">
                                    <td class="text-start">
                                        <div class="d-flex flex-column">
                                            <span><t t-esc="line.product_id ? line.product_id[1] : 'Unknown'"/></span>
                                            <t t-if="line.is_kit">
                                                <span class="badge bg-warning bg-opacity-25 text-warning-emphasis border border-warning border-opacity-50 align-self-start mt-1" style="font-size: 10px;">
                                                    <i class="fa fa-gift me-1"></i> Combo (Kit)
                                                </span>
                                            </t>
                                        </div>
                                    </td>
                                     <td class="fw-bold"><t t-esc="this.formatQty(line.product_uom_qty)"/></td>
                                     <td t-att-class="line.qty_packed &gt;= line.product_uom_qty ? 'text-success fw-bold' : (line.qty_packed &gt; 0 ? 'text-info fw-bold' : 'text-muted')">
                                         <t t-if="line.qty_packed &gt; 0">
                                             <i class="fa fa-cube me-1"></i><t t-esc="this.formatQty(line.qty_packed)"/>
                                             <i t-if="line.qty_packed &gt;= line.product_uom_qty" class="fa fa-check-circle ms-1"></i>
                                         </t>
                                         <t t-else="">0</t>
                                     </td>
                                     <td t-att-class="line.qty_available > 0 ? 'text-success fw-bold' : 'text-danger'">
                                         <t t-esc="this.formatQty(line.qty_available)"/>
                                     </td>
                                    <td class="text-primary"><t t-esc="this.formatQty(line.qty_delivered)"/></td>
                                    <td t-attf-class="fw-bold #{(!line.is_kit &amp;&amp; (line.product_uom_qty - line.qty_delivered) &gt; 0) ? 'text-danger' : 'text-muted opacity-50'}">
                                        <t t-if="line.is_kit">
                                            <span class="small">-</span>
                                        </t>
                                        <t t-else="">
                                            <t t-esc="this.formatQty(line.product_uom_qty - line.qty_delivered)"/>
                                        </t>
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                    
                    <div t-if="state.selectedOrder.pos and state.selectedOrder.pos.length > 0">
                        <h6 class="text-muted text-uppercase small mb-3"><i class="fa fa-shopping-cart me-1"></i> Theo Dõi Đơn Mua (PMH)</h6>
                        <div class="list-group shadow-sm border mb-4">
                            <t t-foreach="state.selectedOrder.pos" t-as="po" t-key="po.id">
                                <div class="list-group-item list-group-item-action d-flex justify-content-between align-items-center p-3 cursor-pointer" t-on-click="() => this.openPurchaseOrder(po.id)">
                                    <div>
                                        <div class="fw-bold"><t t-esc="po.name"/></div>
                                        <div class="small text-muted"><i class="fa fa-user me-1"></i> <t t-esc="po.partner_id ? po.partner_id[1] : 'Unknown'"/></div>
                                    </div>
                                    <div class="text-end">
                                        <span class="badge" t-att-class="this.getPOStatusBadgeClass(po.state, po.receipt_status)">
                                            <t t-esc="this.translatePOStatus(po.receipt_status || po.state)"/>
                                        </span>
                                        <div class="small mt-1 fw-bold" t-if="po.date_planned"><i class="fa fa-clock-o"></i> Dự kiến: <t t-esc="po.date_planned.substring(0, 10)"/></div>
                                    </div>
                                </div>
                            </t>
                        </div>
                    </div>

                    <div t-if="state.selectedOrder.pickings and state.selectedOrder.pickings.length > 0">
                        <h6 class="text-muted text-uppercase small mb-3"><i class="fa fa-sitemap me-1"></i> Luồng Phiếu Xuất Kho</h6>
                        <div class="position-relative ms-3 border-start border-2 border-primary pb-1 mb-4">
                            <t t-foreach="state.selectedOrder.pickings" t-as="picking" t-key="picking.id">
                                <div class="position-relative mb-3 ms-4 cursor-pointer" t-on-click="() => this.openPicking(picking.id)">
                                    <!-- Timeline dot -->
                                    <span class="position-absolute rounded-circle" 
                                          t-att-class="picking.state == 'done' ? 'bg-success' : 'bg-primary'"
                                          style="width: 14px; height: 14px; left: -32px; top: 6px; border: 2px solid white;"></span>
                                          
                                    <!-- Card Content -->
                                    <div class="card shadow-sm border-1" t-att-class="picking.state == 'done' ? 'bg-light opacity-75' : 'bg-white border border-primary'">
                                        <div class="card-body p-3 d-flex justify-content-between align-items-center">
                                            <div>
                                                <div class="small fw-bold mb-1" t-att-class="picking.state == 'done' ? 'text-secondary' : 'text-primary'"><t t-esc="picking.type_name"/></div>
                                                <div class="fw-bold" t-att-class="picking.state == 'done' ? 'text-muted' : 'text-dark'"><i class="fa fa-barcode text-muted me-1"></i> <t t-esc="picking.name"/></div>
                                                
                                                <!-- Return connection -->
                                                <div t-if="picking.return_of" class="small mt-1 text-danger"><i class="fa fa-undo me-1"></i> Trả cho lệnh: <t t-esc="picking.return_of"/></div>
                                                
                                                <!-- Backorder connection -->
                                                <div t-if="picking.backorder_of" class="small mt-1 text-muted"><i class="fa fa-files-o me-1"></i> Phiếu cắt ngang từ: <t t-esc="picking.backorder_of"/></div>

                                                <div class="small text-muted mt-1" t-if="picking.scheduled_date"><i class="fa fa-clock-o me-1"></i> Lịch giao: <t t-esc="picking.scheduled_date"/></div>
                                                
                                                <!-- Video Badge -->
                                                <div t-if="picking.videos and picking.videos.length > 0" class="mt-2 text-start">
                                                    <t t-foreach="picking.videos" t-as="video" t-key="video.id">
                                                        <span class="badge bg-danger cursor-pointer d-inline-flex align-items-center mb-1 me-1 px-2 py-1" 
                                                              t-on-click.stop="() => this.openVideo(video.url)">
                                                            <i class="fa fa-play-circle me-1"></i> Video Đóng Gói
                                                        </span>
                                                    </t>
                                                </div>
                                            </div>
                                            <div class="text-end">
                                                <span class="badge" t-att-class="this.getPickingStatusBadgeClass(picking.state)">
                                                    <t t-esc="this.translatePickingStatus(picking.state)"/>
                                                </span>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>
                    </div>
                </div>
                <!-- Offcanvas Footer -->
                <div class="p-3 bg-white border-top d-flex justify-content-between align-items-center" style="position: absolute; bottom: 0; width: 100%;">
                    <div class="text-muted small"><strong>Trị giá: </strong> <span class="text-primary fs-6 fw-bold"><t t-esc="this.formatCurrency(state.selectedOrder.amount_total)"/></span></div>
                    <button type="button" class="btn btn-primary" t-on-click="() => { this.openSaleOrder(state.selectedOrder.id); this.closeOverviewDrawer(); }">Mở Đơn Bán</button>
                </div>
            </div>

            <!-- Modal Chi Tiết Kiện Hàng (Package Details) -->
            <div t-if="state.isPackageModalOpen" class="modal show d-block" tabindex="-1" style="background: rgba(0,0,0,0.5); z-index: 1060;">
                <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content shadow-lg border-0">
                        <div class="modal-header bg-light text-white p-3">
                            <h5 class="modal-title m-0 "><i class="fa fa-archive me-2"></i> Chi Tiết Kiện: <t t-esc="state.selectedPackage.name"/></h5>
                            <button type="button" class="btn-close btn-close-white" t-on-click="closePackageDetails"></button>
                        </div>
                        <div class="modal-body p-4 bg-light">
                            <div class="row g-3 mb-4">
                                <div class="col-6">
                                    <div class="bg-white rounded p-3 border shadow-sm h-100">
                                        <div class="small text-muted text-uppercase fw-bold mb-1">Thứ tự đóng gói</div>
                                        <div class="fw-bold text-primary"><i class="fa fa-sort-amount-asc me-2"></i> Kiện thứ <t t-esc="state.selectedPackage.sequence"/> / <t t-esc="state.selectedPackage.total"/></div>
                                    </div>
                                </div>
                                <div class="col-6">
                                    <div class="bg-white rounded p-3 border shadow-sm h-100">
                                        <div class="small text-muted text-uppercase fw-bold mb-1">Vị trí hiện tại</div>
                                        <div class="fw-bold text-success"><i class="fa fa-map-marker me-2"></i> <t t-esc="state.selectedPackage.location_name || 'Không rõ'"/></div>
                                    </div>
                                </div>
                            </div>

                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <h6 class="text-muted text-uppercase small m-0 fw-bold"><i class="fa fa-list me-1"></i> Danh sách hàng trong kiện</h6>
                                <button class="btn btn-primary btn-sm rounded-pill shadow-sm px-3" t-on-click="() => this.printPackageLabel(state.selectedPackage.id)">
                                    <i class="fa fa-print me-2"></i> In Tem Kiện
                                </button>
                            </div>
                            <div class="table-responsive rounded border bg-white">
                                <table class="table table-sm table-hover mb-0">
                                    <thead class="table-light">
                                        <tr>
                                            <th class="ps-3">Sản phẩm</th>
                                            <th class="text-end pe-3" width="80">SL đóng</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <t t-foreach="Object.entries(state.selectedPackage.product_map)" t-as="entry" t-key="entry[0]">
                                            <tr>
                                                <td class="ps-3"><t t-esc="entry[0]"/></td>
                                                <td class="text-end pe-3 fw-bold text-primary"><t t-esc="this.formatQty(entry[1])"/></td>
                                            </tr>
                                        </t>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        <div class="modal-footer bg-light border-top p-2">
                            <button type="button" class="btn btn-secondary" t-on-click="closePackageDetails">Đóng</button>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Backdrop Drawer -->
            <div t-if="state.isDrawerOpen" class="modal-backdrop fade show" t-on-click="closeOverviewDrawer" style="z-index: 1040;"></div>
            
        </div>
    </t>
</templates>
