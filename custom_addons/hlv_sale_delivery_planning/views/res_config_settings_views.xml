<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_delivery_planner_pack" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.delivery.planner.pack</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="HLV Delivery Planner" string="HLV Delivery Planner" name="hlv_sale_delivery_planning">
                    <block title="In phiếu lấy hàng (IoT)" name="hlv_delivery_planner_pick_print_lock_settings">
                        <setting string="Khóa mở/gửi in phiếu lấy hàng"
                                 help="Khi bật, sale KHÔNG xem trước / gửi yêu cầu in phiếu lấy hàng được trên trang sale plan. Dùng để khóa tạm tính năng trước khi chính thức vận hành, tắt lại khi sẵn sàng.">
                            <field name="lock_pick_slip_requests"/>
                        </setting>
                        <setting string="Tự động gửi in khi phiếu đủ hàng"
                                 help="Khi bật, ngay khi phiếu lấy hàng giữ ĐỦ hàng cho TẤT CẢ sản phẩm, hệ thống tự động gửi yêu cầu in vào hàng chờ theo kho — sale không cần bấm gửi in. Mỗi phiếu chỉ tự động gửi 1 lần.">
                            <field name="auto_print_pick_slip_when_full"/>
                        </setting>
                    </block>
                    <block title="Đóng gói" name="hlv_delivery_planner_pack_settings">
                        <setting string="Chỉ người được assign mới được đóng gói"
                                 help="Khi bật, người không được chọn lúc in phiếu lấy hàng sẽ không vào/validate được phiếu PACK. Quản lý kho và admin được bỏ qua.">
                            <field name="restrict_pack_to_assigned_user"/>
                        </setting>
                        <setting string="Chế độ ghi nhận thời gian in phiếu"
                                 help="'Lần đầu in': chỉ ghi nhận thời gian in đầu tiên. 'Lần in gần nhất': luôn cập nhật với mỗi lần in mới.">
                            <field name="pick_print_time_mode" widget="radio"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>
</odoo>
