<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_tree_inherit_print_count" model="ir.ui.view">
        <field name="name">sale.order.tree.inherit.print.count</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree"/>
        <field name="arch" type="xml">
            <field name="state" position="before">
                <field name="picking_slip_print_count" optional="show" sum="Total"/>
            </field>
        </field>
    </record>

    <record id="view_sales_order_filter_inherit_print" model="ir.ui.view">
        <field name="name">sale.order.list.select.inherit.print</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter"/>
        <field name="arch" type="xml">
            <filter name="my_sale_orders_filter" position="after">
                <separator/>
                <filter string="Chưa in phiếu" name="filter_not_printed" domain="[('picking_slip_print_count', '=', 0)]"/>
                <filter string="Đã in phiếu" name="filter_printed" domain="[('picking_slip_print_count', '>', 0)]"/>
                <separator/>
            </filter>
        </field>
    </record>

    <record id="action_print_batch_picking_slip" model="ir.actions.server">
        <field name="name">In Phiếu Lấy Hàng (Có đếm)</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
            action = records.action_print_batch_picking_slip()
        </field>
    </record>
</odoo>