<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Client Action cho Dashboard OWL (tự viết, không dùng pivot/graph mặc định) -->
    <record id="action_misa_invoice_dashboard" model="ir.actions.client">
        <field name="name">Tổng quan</field>
        <field name="tag">misa_invoice_status_report.Dashboard</field>
        <field name="target">main</field>
    </record>

    <!-- Trang riêng "Danh sách đơn hàng" (khác dashboard Tổng quan) — mở từ nút "Xem tất cả
         đơn hàng", chỉ có list + drawer thể hiện quan hệ đơn hàng ↔ phiếu xuất kho. -->
    <record id="action_misa_invoice_order_list_page" model="ir.actions.client">
        <field name="name">Danh sách đơn hàng</field>
        <field name="tag">misa_invoice_status_report.OrderListPage</field>
        <field name="target">main</field>
    </record>
</odoo>
