<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="hlv_barcode_shipper.RouteMap" owl="1">
        <div class="hlv-route-map" t-ref="map"/>
    </t>

    <t t-name="hlv_barcode_shipper.RouteStopList" owl="1">
        <div class="hlv-route-sheet">
            <div class="hlv-route-sheet-handle"/>
            <div class="hlv-route-next">
                <span>ĐIỂM TIẾP THEO</span>
                <strong>Cách <t t-esc="formatDistance(props.nextDistance || 0)"/></strong>
            </div>
            <div class="hlv-route-stop-list">
                <t t-foreach="props.stops || []" t-as="stop" t-key="stop.id">
                    <article class="hlv-route-stop"
                             t-att-draggable="!props.started"
                             t-on-dragstart="(ev) => this.onDragStart(stop_index, ev)"
                             t-on-dragover="onDragOver"
                             t-on-drop="(ev) => this.onDrop(stop_index, ev)">
                        <div class="hlv-stop-order"><t t-esc="stop_index + 1"/></div>
                        <div class="hlv-stop-body">
                            <div class="hlv-stop-title">
                                <strong><t t-esc="stop.picking_name"/></strong>
                                <span class="hlv-stop-badge"><i class="fa fa-box"></i> <t t-esc="stop.item_count"/> kiện</span>
                            </div>
                            <div class="hlv-stop-partner"><t t-esc="stop.partner_name"/></div>
                            <div class="hlv-stop-address"><i class="fa fa-map-marker-alt"></i> <t t-esc="stop.address"/></div>
                        </div>
                        <i class="fa fa-grip-lines hlv-stop-grip" t-if="!props.started"></i>
                    </article>
                </t>
            </div>
        </div>
    </t>

    <t t-name="hlv_barcode_shipper.DeliveryRouteApp" owl="1">
        <main class="hlv-delivery-route" t-att-class="{ 'is-started': state.started }">
            <t t-if="state.isLoading">
                <div class="hlv-route-loading">
                    <i class="fa fa-spinner fa-spin"></i>
                    <span>Đang tải tuyến giao hàng...</span>
                </div>
            </t>
            <t t-else="">
                <t t-if="state.errorMessage">
                    <div class="hlv-route-empty">
                        <i class="fa fa-route"></i>
                        <strong><t t-esc="state.errorMessage"/></strong>
                        <a class="btn btn-primary" t-att-href="state.scannerUrl">Mở giao hàng</a>
                    </div>
                </t>
                <t t-else="">
                    <section class="hlv-route-map-wrap">
                        <RouteMap apiKey="state.apiKey"
                                  origin="state.origin"
                                  stops="state.routeStops"
                                  started="state.started"
                                  onRouteSummary="onRouteSummary.bind(this)"
                                  onError="onMapError.bind(this)"/>
                    </section>

                    <div class="hlv-route-topbar">
                        <button class="hlv-route-icon-btn" t-on-click="goBack" title="Quay lại">
                            <i class="fa fa-arrow-left"></i>
                        </button>
                        <div class="hlv-route-summary"><t t-esc="routeSummaryText"/></div>
                        <button class="hlv-route-icon-btn" t-on-click="bootstrapRoute.bind(this)" title="Tải lại">
                            <i class="fa fa-sync-alt"></i>
                        </button>
                    </div>

                    <t t-if="state.warningMessage">
                        <div class="hlv-route-warning"><t t-esc="state.warningMessage"/></div>
                    </t>

                    <RouteStopList stops="state.routeStops"
                                   started="state.started"
                                   nextDistance="nextDistance"
                                   onReorder="onReorder.bind(this)"/>

                    <button t-if="!state.started"
                            class="hlv-route-start-btn"
                            t-on-click="startDelivery">
                        <i class="fa fa-location-arrow"></i> Bắt đầu giao hàng
                    </button>

                    <button t-if="state.started"
                            class="hlv-route-fab"
                            t-att-style="'transform: translateX(' + state.fabDragX + 'px)'"
                            t-on-click="openScanner"
                            t-on-touchstart="onFabTouchStart"
                            t-on-touchmove="onFabTouchMove"
                            t-on-touchend="onFabTouchEnd"
                            title="Giao hàng">
                        <i class="fa fa-truck"></i>
                        <span>Giao hàng</span>
                    </button>
                </t>
            </t>
        </main>
    </t>
</templates>
