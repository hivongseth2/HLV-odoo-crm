<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_picking_form_inherit_smart_print" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit.smart.print</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[1]" position="before">
                <button name="action_smart_print" string="🖨 In biên bản" type="object" class="btn-primary" 
                        invisible="state == 'draft'"/>
            </xpath>
        </field>
    </record>

    <record id="action_server_smart_print_batch" model="ir.actions.server">
        <field name="name">In biên bản thông minh</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="binding_view_types" >list</field>
        <field name="state">code</field>
        <field name="code">
for record in records:
    record.action_smart_print()
        </field>
    </record>
</odoo>
