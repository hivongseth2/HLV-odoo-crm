<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <template id="inventory_page" name="Public Inventory Page">
    <t t-call="website.layout">
      <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&amp;display=swap');

        /* ============================================================
           Public Inventory UI v5 — single source, no duplication
           Sections: Vars · Layout · Typography · Tabs · Search
                     Buttons · Cards · WH-detail · Modal · Desktop · Mobile
           ============================================================ */

        /* ── CSS variables ── */
        :root {
          --inv-p:       #2563eb;
          --inv-p-soft:  #eff6ff;
          --inv-p-ring:  rgba(37,99,235,.15);
          --inv-tx:      #1e293b;
          --inv-mu:      #64748b;
          --inv-su:      #94a3b8;
          --inv-bd:      #e2e8f0;
          --inv-bd-s:    #f1f5f9;
          --inv-bg:      #f8fafc;
          --inv-ok:      #15803d;
          --inv-ok-bg:   #dcfce7;
          --inv-err:     #b91c1c;
          --inv-err-bg:  #fee2e2;
          --inv-warn:    #a16207;
          --inv-warn-bg: #fef3c7;
          --inv-sh-sm:   0 1px 2px rgba(15,23,42,.05);
          --inv-sh-md:   0 10px 24px -6px rgba(15,23,42,.10);
          --inv-sh-gl:   0 16px 44px -28px rgba(15,23,42,.36), inset 0 1px 0 rgba(255,255,255,.78);
        }

        /* ── Odoo theme width override ── */
        html #wrapwrap > main#wrap,
        html #wrapwrap > #wrap.oe_structure,
        html #wrapwrap > *:has(#inv-table),
        html #wrapwrap > main#wrap > section,
        html #wrapwrap > #wrap.oe_structure > section,
        body:has(#inv-table) #wrapwrap > * {
          max-width: none !important;
          width: 100% !important;
          margin-left: 0 !important;
          margin-right: 0 !important;
        }

        /* ── Page scroll ── */
        body:has(#inv-table),
        body:has(#inv-table) #wrapwrap,
        body:has(#inv-table) #wrap {
          height: auto !important;
          min-height: 100vh !important;
          overflow-y: auto !important;
          overflow-x: hidden !important;
        }

        /* ── Page base ── */
        #wrap {
          font-family: "Inter","Segoe UI",system-ui,-apple-system,sans-serif;
          font-size: 14px;
          font-weight: 400;
          line-height: 1.5;
          letter-spacing: -.01em;
          color: var(--inv-tx);
          background: linear-gradient(135deg,rgba(37,99,235,.05) 0%,transparent 30%),
                      linear-gradient(180deg,#f8fafc 0%,#eef4fb 50%,#f8fafc 100%);
          min-height: 100vh;
          -webkit-font-smoothing: antialiased;
        }
        #wrap input,#wrap select,#wrap button,#wrap textarea,#wrap table {
          font-family: inherit;
          letter-spacing: inherit;
        }
        #wrap .container-fluid {
          width: min(100%,calc(100vw - 20px)) !important;
          max-width: 1900px !important;
          padding: 12px 10px 32px !important;
          margin: 0 auto !important;
          color: var(--inv-tx);
        }

        /* ── Typography ── */
        #wrap .text-dark,#wrap .fw-bold,#wrap label,#wrap .form-label { color: var(--inv-tx) !important; }
        #wrap .text-muted,#wrap small { color: var(--inv-mu) !important; }
        #wrap .text-primary,#wrap a.text-primary,#wrap .btn-forecast-detail { color: var(--inv-p) !important; }
        #wrap .fw-bold,#wrap .fw-semibold,#wrap strong,
        #wrap [style*="font-weight:7"],#wrap [style*="font-weight: 7"],
        #wrap [style*="font-weight:8"],#wrap [style*="font-weight: 8"],
        #wrap [style*="font-weight:bold"],#wrap [style*="font-weight: bold"] { font-weight: 600 !important; }

        /* ── Tab strip ── */
        #wrap .container-fluid > div:first-child {
          position: sticky; top: 6px; z-index: 1032;
          padding: 10px 12px; margin-bottom: 10px;
          border: 1px solid rgba(226,232,240,.7); border-radius: 4px;
          background: rgba(255,255,255,.82);
          box-shadow: var(--inv-sh-gl); backdrop-filter: blur(18px) saturate(145%);
        }
        #wrap .nav.nav-pills { gap: 6px !important; }
        #wrap .nav-pills .nav-link {
          border: 1px solid rgba(226,232,240,.7) !important; border-radius: 999px !important;
          background: rgba(255,255,255,.5) !important; color: var(--inv-mu) !important;
          font-size: .88rem !important; font-weight: 500 !important; box-shadow: none !important;
          transition: color .15s,background .15s,border-color .15s;
        }
        #wrap .nav-pills .nav-link:hover {
          color: var(--inv-p) !important; background: var(--inv-p-soft) !important; border-color: #dbeafe !important;
        }
        #wrap .nav-pills .nav-link.active,#wrap .nav-pills .show > .nav-link {
          color: var(--inv-p) !important; background: rgba(239,246,255,.95) !important;
          border-color: rgba(147,197,253,.8) !important;
          box-shadow: 0 6px 14px -12px rgba(37,99,235,.65) !important;
        }

        /* ── Search form ── */
        #search-form {
          position: sticky; top: 74px; z-index: 1031;
          display: grid;
          grid-template-columns: minmax(300px,1.4fr) minmax(150px,.55fr) minmax(220px,.7fr) minmax(130px,.35fr);
          gap: 8px !important; align-items: center;
          padding: 10px !important; margin-bottom: 12px !important;
          border: 1px solid rgba(226,232,240,.72); border-radius: 4px;
          background: rgba(255,255,255,.82);
          box-shadow: var(--inv-sh-gl); backdrop-filter: blur(18px) saturate(145%);
        }
        #search-form > [class*="col-"] {
          width: auto !important; max-width: none !important;
          flex: none !important; padding: 0 !important; margin: 0 !important;
        }
        .search-wrapper,#search-form .form-select,#search-form .form-control {
          height: 40px; min-height: 40px;
          border: 1px solid rgba(203,213,225,.8) !important; border-radius: 4px !important;
          background: rgba(255,255,255,.75) !important; color: var(--inv-tx) !important;
          backdrop-filter: blur(10px); transition: border-color .15s,box-shadow .15s;
        }
        .search-wrapper {
          display: flex; flex-wrap: wrap; align-items: center;
          gap: 5px; padding: 4px 9px; cursor: text;
        }
        .search-wrapper:focus-within,
        #search-form .form-select:focus,
        #search-form .form-control:focus {
          border-color: var(--inv-p) !important;
          box-shadow: 0 0 0 3px var(--inv-p-ring) !important; outline: none !important;
        }
        .search-input-field {
          border: none; outline: none; flex-grow: 1; min-width: 90px;
          font-size: 14px; background: transparent; color: var(--inv-tx) !important;
        }
        .search-input-field::placeholder { color: var(--inv-su); }
        .search-chip {
          display: inline-flex; align-items: center; gap: 4px;
          padding: 2px 8px; background: var(--inv-p-soft) !important;
          color: #1d4ed8 !important; border: 1px solid #bfdbfe;
          border-radius: 999px; font-size: .84rem; font-weight: 500; user-select: none;
        }
        .search-chip span { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; max-width: 240px; }
        .search-chip .btn-close-chip { cursor: pointer; color: #60a5fa !important; font-weight: 600; font-size: 15px; line-height: 1; }
        #search-form .col-md-3 {
          display: flex; align-items: center; gap: 10px;
          min-height: 40px; padding: 6px 9px !important;
          border: 1px solid rgba(226,232,240,.7); border-radius: 4px;
          background: rgba(248,250,252,.65) !important;
        }
        #search-form .form-check-label { color: var(--inv-mu) !important; font-size: .87rem; font-weight: 500; }
        #search-form .form-check-input { border-color: #cbd5e1; box-shadow: none !important; }
        #search-form .form-check-input:checked { background-color: var(--inv-p); border-color: var(--inv-p); }
        #search-suggestions {
          border: 1px solid var(--inv-bd) !important; border-radius: 4px !important;
          box-shadow: var(--inv-sh-md) !important; overflow: hidden;
          background: rgba(255,255,255,.95) !important; backdrop-filter: blur(14px); max-height: 60vh;
        }
        #search-suggestions .suggestion-item {
          border-bottom-color: var(--inv-bd-s) !important; cursor: pointer; transition: background .13s;
        }
        #search-suggestions .suggestion-item:hover { background: var(--inv-bg); }

        /* ── Buttons ── */
        #wrap .btn {
          border-radius: 4px !important; font-weight: 500 !important; box-shadow: var(--inv-sh-sm);
          transition: transform .12s,box-shadow .14s,background .14s;
        }
        #wrap .btn:hover { transform: translateY(-1px); box-shadow: 0 4px 10px rgba(15,23,42,.08); }
        #wrap .btn-primary,#search-form .btn-primary {
          background: linear-gradient(180deg,#3b82f6,#2563eb) !important;
          border-color: rgba(37,99,235,.9) !important; color: #fff !important; height: 40px;
          box-shadow: 0 8px 20px -14px rgba(37,99,235,.85) !important;
        }
        #wrap .btn-light,#wrap .btn-secondary {
          background: #f8fafc !important; border: 1px solid var(--inv-bd) !important; color: var(--inv-tx) !important;
        }
        #wrap .btn-expand {
          width: 28px; height: 28px; display: inline-flex;
          align-items: center; justify-content: center; border-radius: 999px !important;
          color: var(--inv-p) !important; background: var(--inv-p-soft) !important;
          border: 1px solid #dbeafe !important; box-shadow: none;
        }

        /* ── Generic card ── */
        #wrap .card {
          border: 1px solid rgba(226,232,240,.76) !important; border-radius: 4px !important;
          box-shadow: var(--inv-sh-gl) !important; background: rgba(255,255,255,.82) !important;
          backdrop-filter: blur(14px) saturate(140%);
        }

        /* ── Warehouse detail components (shared desktop + mobile base) ── */
        .wh-stats { display: flex; padding: 8px 0; }
        .wh-stat-col {
          flex: 1; display: flex; flex-direction: column;
          align-items: center; justify-content: center; border-right: 1px solid #f0f0f0;
        }
        .wh-stat-col:last-child { border-right: none; }
        .wh-label { font-size: .7rem; text-transform: uppercase; color: var(--inv-su) !important; font-weight: 600 !important; margin-bottom: 2px; }
        .wh-val   { font-size: 1rem; font-weight: 600 !important; color: var(--inv-tx) !important; }
        .wh-stat-col.highlight .wh-label { color: var(--inv-ok) !important; opacity: .85; }
        .wh-stat-col.highlight .wh-val   { color: var(--inv-ok) !important; }
        .wh-stat-col.dim .wh-val { color: #ced4da !important; }
        .combo-comp-title { font-size: .88rem; font-weight: 600; color: #d63384 !important; margin-bottom: 4px; padding-left: 4px; }

        /* ── Modals ── */
        #forecastDetailsModal .modal-content,
        #locationDetailsModal .modal-content {
          border: 1px solid rgba(226,232,240,.85) !important; border-radius: 4px !important;
          box-shadow: var(--inv-sh-md) !important; overflow: hidden;
          color: var(--inv-tx); background: rgba(255,255,255,.92) !important; backdrop-filter: blur(14px);
        }
        #forecastDetailsModal .modal-header,#locationDetailsModal .modal-header {
          background: #fff !important; border-bottom: 1px solid var(--inv-bd-s) !important; padding: 14px 18px !important;
        }
        #forecastDetailsModal .modal-title,#locationDetailsModal .modal-title,
        #forecastDetailsModal [style*="font-weight:7"],#locationDetailsModal [style*="font-weight:7"] {
          color: var(--inv-tx) !important; font-weight: 600 !important;
        }
        #forecastDetailsModal .modal-body,#locationDetailsModal .modal-body { background: #fff; }
        #forecastDetailsModal .modal-footer,#locationDetailsModal .modal-footer {
          background: var(--inv-bg) !important; border-top: 1px solid var(--inv-bd-s) !important; padding: 9px 18px !important;
        }
        #forecastDetailsModal .btn-close,#locationDetailsModal .btn-close {
          border-radius: 999px; opacity: .52; transition: opacity .13s;
        }
        #forecastDetailsModal .btn-close:hover,#locationDetailsModal .btn-close:hover { opacity: .9; }
        #forecastDetailsBody > div:first-child {
          background: var(--inv-bg) !important; border-bottom: 1px solid var(--inv-bd-s) !important;
        }
        #forecastDetailsBody > div:first-child > div:first-child { gap: 8px !important; }
        #forecastDetailsBody > div:first-child > div:first-child > div[style*="border-radius"] {
          border: 1px solid var(--inv-bd-s) !important; border-radius: 4px !important;
          box-shadow: var(--inv-sh-sm) !important; background: #fff !important; min-width: 100px !important;
        }
        #forecastDetailsBody > div:first-child > div:first-child > span {
          color: var(--inv-mu) !important; font-weight: 600 !important;
        }
        #forecastDetailsBody .fc-table { color: var(--inv-tx) !important; font-size: .83rem !important; }
        #forecastDetailsBody .fc-table th {
          background: var(--inv-bg) !important; color: var(--inv-mu) !important;
          border-bottom: 1px solid var(--inv-bd) !important; font-weight: 500 !important; padding: 9px 11px !important;
        }
        #forecastDetailsBody .fc-table td { border-bottom: 1px solid var(--inv-bd-s) !important; padding: 10px !important; }
        #forecastDetailsBody .fc-table tr:hover td { background: var(--inv-bg) !important; }
        #forecastDetailsBody .fc-badge { border-radius: 4px !important; font-weight: 600 !important; padding: 2px 8px !important; }

        /* ============================================================
           DESKTOP &gt;= 769px
           ============================================================ */
        @media (min-width: 769px) {
          #wrap .card:has(#inv-table) {
            padding: 0 !important; border: 0 !important;
            background: transparent !important; box-shadow: none !important;
          }
          .table-responsive {
            overflow: auto !important; 
            border: 1px solid rgba(226,232,240,.76); border-radius: 4px;
            background: rgba(255,255,255,.78); box-shadow: var(--inv-sh-gl);
            backdrop-filter: blur(18px) saturate(145%);
            scrollbar-width: thin; scrollbar-color: rgba(148,163,184,.55) transparent;
            overscroll-behavior: contain;
          }
          .table-responsive::-webkit-scrollbar { width: 5px; height: 7px; }
          .table-responsive::-webkit-scrollbar-track { background: transparent; }
          .table-responsive::-webkit-scrollbar-thumb { background: rgba(148,163,184,.44); border-radius: 999px; }
          #inv-table {
            min-width: 1380px !important; width: 100%;
            border-collapse: separate !important; border-spacing: 0 !important;
            border: 0 !important; background: transparent !important;
            color: var(--inv-tx); font-size: 13px; line-height: 1.5;
          }
          #inv-table th,#inv-table td {
            padding: 11px 13px !important; vertical-align: middle !important;
            text-align: center !important;
            border: 0 !important; border-bottom: 1px solid rgba(226,232,240,.6) !important;
          }
          #inv-table tbody tr.main-row td:nth-child(4) { text-align: left !important; }
          #inv-table thead th {
            position: sticky !important; top: 0 !important; z-index: 30 !important;
            height: 44px; white-space: nowrap;
            background: rgba(248,250,252,.95) !important; color: var(--inv-mu) !important;
            font-size: 10.5px !important; font-weight: 600 !important;
            letter-spacing: .02em !important; text-transform: uppercase;
            box-shadow: 0 1px 0 var(--inv-bd), 0 10px 18px -22px rgba(15,23,42,.9) !important;
            backdrop-filter: blur(18px) saturate(150%) !important;
          }
          #inv-table .detail-row thead th,
          #inv-table .detail-container-mobile thead th {
            position: static !important; height: auto; font-size: 12px !important;
            box-shadow: none !important; backdrop-filter: none !important;
          }
          #inv-table tbody tr.main-row { background: rgba(255,255,255,.38) !important; transition: background .14s; }
          #inv-table tbody tr.main-row:hover { background: rgba(255,255,255,.78) !important; cursor: pointer; }
          #inv-table tbody tr.main-row:hover td { background: transparent !important; }
          #inv-table tbody tr.main-row td:nth-child(1),
          #inv-table tbody tr.main-row td:nth-child(2),
          #inv-table tbody tr.main-row td:nth-child(14) { width: 1%; white-space: nowrap; }
          #inv-table tbody tr.main-row td:nth-child(3) { color: var(--inv-p) !important; font-weight: 600 !important; white-space: nowrap; }
          #inv-table tbody tr.main-row td:nth-child(4) .text-wrap {
            min-width: 220px !important; max-width: 400px; color: var(--inv-tx) !important; font-weight: 600 !important;
          }
          #inv-table tbody tr.main-row td:nth-child(5),
          #inv-table tbody tr.main-row td:nth-child(6),
          #inv-table tbody tr.main-row td:nth-child(9),
          #inv-table tbody tr.main-row td:nth-child(10),
          #inv-table tbody tr.main-row td:nth-child(11),
          #inv-table tbody tr.main-row td:nth-child(12),
          #inv-table tbody tr.main-row td:nth-child(13) { color: var(--inv-mu) !important; font-weight: 400 !important; }
          #inv-table tbody tr.main-row td:nth-child(2) img { border-radius: 4px; border: 1px solid var(--inv-bd-s); background: #fff; }
          #inv-table tbody tr.main-row td:nth-child(8) > span,
          #inv-table tbody tr.main-row td:nth-child(7) a {
            display: inline-flex; align-items: center; justify-content: center;
            min-height: 22px; padding: 1px 7px; border-radius: 4px;
            font-weight: 600 !important; line-height: 1.2;
          }
          #inv-table tbody tr.main-row td:nth-child(8) > span.text-success { color: var(--inv-ok) !important; background: var(--inv-ok-bg) !important; }
          #inv-table tbody tr.main-row td:nth-child(8) > span.text-danger  { color: var(--inv-err) !important; background: var(--inv-err-bg) !important; }
          #inv-table td.text-success { color: var(--inv-ok) !important; }
          #inv-table td.text-danger  { color: var(--inv-err) !important; }
          #inv-table tbody tr.main-row td:nth-child(7) a { color: var(--inv-mu) !important; background: var(--inv-bg); }
          #inv-table .badge { border-radius: 999px; font-weight: 600 !important; box-shadow: none !important; }
          #inv-table .bg-warning { background: var(--inv-warn-bg) !important; color: var(--inv-warn) !important; }
          #inv-table tbody tr.detail-row > td {
            padding: 8px 12px !important; background: rgba(248,250,252,.5) !important;
            border-bottom: 1px solid var(--inv-bd) !important;
          }
          .detail-container-mobile {
            background: rgba(248,250,252,.8); border: 1px solid var(--inv-bd-s);
            border-radius: 4px;
          }
          .wh-card,.detail-container-mobile .card {
            border: 1px solid var(--inv-bd-s) !important; border-radius: 4px !important;
            box-shadow: var(--inv-sh-sm) !important; overflow: hidden; margin-bottom: 8px;
          }
          .wh-header,.detail-container-mobile .card-header {
            padding: 8px 11px; background: #fff !important;
            border-bottom: 1px solid var(--inv-bd-s) !important; font-weight: 600 !important; color: var(--inv-tx) !important;
          }
          .wh-stat-col.highlight .wh-val {
            background: var(--inv-ok-bg); border-radius: 4px; padding: 1px 8px;
            display: inline-flex; align-items: center; justify-content: center; min-height: 22px;
          }
          .detail-container-mobile .table,
          .detail-container-mobile table { color: var(--inv-tx); border-color: var(--inv-bd-s) !important; }
          .detail-container-mobile thead th {
            background: var(--inv-bg) !important; color: var(--inv-mu) !important;
            font-weight: 500 !important; box-shadow: none !important;
          }
          .detail-container-mobile td,
          .detail-container-mobile th { border-color: var(--inv-bd-s) !important; }
        }

        /* Tablet: 2-col search bar */
        @media (min-width: 769px) and (max-width: 1199.98px) {
          #search-form { grid-template-columns: 1fr 1fr; top: 70px; }
        }

        /* ============================================================
           MOBILE &lt;= 768px
           ============================================================ */
        @media (max-width: 768px) {
          /* Natural page scroll */
          body:has(#inv-table),
          body:has(#inv-table) #wrapwrap,
          body:has(#inv-table) #wrap,
          body:has(#inv-table) main,
          body:has(#inv-table) section,
          body:has(#inv-table) .container-fluid,
          body:has(#inv-table) .card,
          body:has(#inv-table) .table-responsive {
            height: auto !important; max-height: none !important; overflow-y: visible !important;
          }
          body:has(#inv-table) { overflow-y: auto !important; -webkit-overflow-scrolling: touch; }
          #wrap .container-fluid { width: 100% !important; max-width: none !important; padding: 8px 8px 80px !important; }
          /* Hide desktop tab bar */
          #wrap .container-fluid > div:first-child { display: none !important; }
          /* Search: single column */
          #search-form {
            display: grid !important; grid-template-columns: 1fr !important;
            position: relative !important; top: auto !important; z-index: 1 !important;
            gap: 8px !important; padding: 10px !important; margin-bottom: 10px !important;
            border: 1px solid rgba(226,232,240,.72) !important; border-radius: 4px !important;
            background: rgba(255,255,255,.82) !important;
            box-shadow: 0 8px 20px -16px rgba(15,23,42,.4) !important; backdrop-filter: blur(12px) !important;
          }
          #search-form > [class*="col-"] { width: 100% !important; max-width: none !important; padding: 0 !important; margin: 0 !important; }
          .search-wrapper,#search-form .form-select,#search-form .btn { height: 40px !important; min-height: 40px !important; }
          #search-form .col-md-3 {
            height: auto !important; min-height: 40px !important;
            padding: 7px 9px !important; justify-content: space-between !important;
          }
          /* Table -> blocks */
          .table-responsive {
            overflow: visible !important; max-height: none !important;
            border: 0 !important; border-radius: 0 !important;
            background: transparent !important; box-shadow: none !important; backdrop-filter: none !important;
          }
          #inv-table,#inv-table tbody {
            display: block !important; width: 100% !important; max-width: 100% !important;
            min-width: 0 !important; border: 0 !important; background: transparent !important;
          }
          #inv-table thead { display: none !important; }
          /* ── Product card ── */
          #inv-table tbody tr.main-row {
            display: grid !important;
            grid-template-columns: 58px minmax(0,1fr) 28px !important;
            grid-template-rows: auto auto auto auto auto auto auto !important;
            gap: 5px 9px !important;
            margin: 0 0 5px !important; padding: 9px !important;
            border: 1px solid var(--inv-bd) !important; border-radius: 4px !important;
            background: #fff !important; box-shadow: 0 1px 2px rgba(15,23,42,.05) !important;
          }
          #inv-table tbody tr.main-row td {
            display: none !important; padding: 0 !important; border: 0 !important;
            background: transparent !important; min-width: 0 !important;
          }
          /* toggle (+/-) */
          #inv-table tbody tr.main-row td:nth-child(1) {
            display: flex !important; position: static !important;
            grid-column: 3 !important; grid-row: 1 !important;
            align-self: start; justify-self: end;
          }
          /* image */
          #inv-table tbody tr.main-row td:nth-child(2) {
            display: block !important; grid-column: 1 !important; grid-row: 1 / 3 !important; width: 58px !important;
          }
          #inv-table tbody tr.main-row td:nth-child(2) > div { width: 52px !important; height: 52px !important; }
          #inv-table tbody tr.main-row td:nth-child(2) img   { border-radius: 3px !important; }
          /* name */
          #inv-table tbody tr.main-row td:nth-child(4) {
            display: block !important; grid-column: 2 / 3 !important; grid-row: 1 / 3 !important;
          }
          #inv-table tbody tr.main-row td:nth-child(4) .text-wrap {
            min-width: 0 !important; max-width: none !important; margin: 0 !important;
            color: var(--inv-tx) !important; font-size: 13px !important; font-weight: 600 !important; line-height: 1.35 !important;
          }
          #inv-table tbody tr.main-row td:nth-child(4) .d-md-none {
            display: block !important; color: var(--inv-p) !important;
            font-size: 11px !important; font-weight: 600 !important; margin-top: 2px !important;
          }
          /* hidden cols */
          #inv-table tbody tr.main-row td:nth-child(3),
          #inv-table tbody tr.main-row td:nth-child(5),
          #inv-table tbody tr.main-row td:nth-child(6),
          #inv-table tbody tr.main-row td:nth-child(7),
          #inv-table tbody tr.main-row td:nth-child(14) { display: none !important; }
          /* price / stock rows */
          #inv-table tbody tr.main-row td:nth-child(8),
          #inv-table tbody tr.main-row td:nth-child(9),
          #inv-table tbody tr.main-row td:nth-child(10),
          #inv-table tbody tr.main-row td:nth-child(11),
          #inv-table tbody tr.main-row td:nth-child(12),
          #inv-table tbody tr.main-row td:nth-child(13) {
            grid-column: 1 / 4 !important; display: flex !important;
            justify-content: space-between !important; align-items: center !important;
            min-height: 25px !important; padding: 3px 0 0 !important;
            border-top: 1px solid var(--inv-bd-s) !important;
            color: var(--inv-tx) !important; font-size: 12.5px !important;
          }
          #inv-table tbody tr.main-row td:nth-child(8) { padding-top: 7px !important; margin-top: 1px !important; }
          #inv-table tbody tr.main-row td:nth-child(8) > span {
            display: inline-flex !important; align-items: center !important;
            min-height: 20px !important; border-radius: 3px !important; padding: 1px 6px !important; font-weight: 600 !important;
          }
          #inv-table tbody tr.main-row td:nth-child(8) > span.text-success { color: var(--inv-ok) !important; background: var(--inv-ok-bg) !important; }
          #inv-table tbody tr.main-row td:nth-child(8) > span.text-danger  { color: var(--inv-err) !important; background: var(--inv-err-bg) !important; }
          /* pseudo labels */
          #inv-table tbody tr.main-row td:nth-child(8):before  { content:"Tổng tồn kho:";  color:var(--inv-mu); font-size:.82rem; }
          #inv-table tbody tr.main-row td:nth-child(9):before  { content:"Giá Web:";        color:var(--inv-mu); font-size:.82rem; }
          #inv-table tbody tr.main-row td:nth-child(10):before { content:"Giá TMĐT:";       color:var(--inv-mu); font-size:.82rem; }
          #inv-table tbody tr.main-row td:nth-child(11):before { content:"Giá niêm yết:";   color:var(--inv-mu); font-size:.82rem; }
          #inv-table tbody tr.main-row td:nth-child(12):before { content:"Giá TM:";         color:var(--inv-mu); font-size:.82rem; }
          #inv-table tbody tr.main-row td:nth-child(13):before { content:"Giá bán:";        color:var(--inv-mu); font-size:.82rem; }
          /* ── Warehouse detail nested under product card ── */
          #inv-table tbody tr.detail-row {
            display: block !important;
            margin: 0 20px !important; padding: 10px !important;
            border: 0 !important; border-left: 2px solid #cbd5e1 !important;
            background: transparent !important;
          }
          #inv-table tbody tr.detail-row:before {
            content: "Tồn theo kho"; display: block; margin-bottom: 5px;
            color: var(--inv-mu); font-size: 10px; font-weight: 600;
            letter-spacing: .04em; text-transform: uppercase;
          }
          #inv-table tbody tr.detail-row > td {
            display: block !important; width: 100% !important;
            padding: 0 !important; border: 0 !important; background: transparent !important;
          }
          .detail-container-mobile {
            display: block !important; max-height: none !important; overflow: visible !important;
            margin: 0 !important; padding: 0 !important;
            border: 0 !important; border-radius: 0 !important;
            background: transparent !important; box-shadow: none !important; backdrop-filter: none !important;
          }
          .wh-card,.detail-container-mobile .card {
            border: 1px solid var(--inv-bd) !important; border-radius: 3px !important;
            background: #f8fafc !important; box-shadow: none !important;
            margin-bottom: 5px !important; overflow: hidden;
          }
          .wh-header,.detail-container-mobile .card-header {
            min-height: 34px; padding: 6px 9px !important; background: #fff !important;
            border-bottom: 1px solid var(--inv-bd-s) !important;
            font-size: 12.5px !important; font-weight: 600 !important; color: var(--inv-tx) !important;
          }
          .wh-header .btn-view-location,.detail-container-mobile .btn-view-location {
            padding: 2px 6px !important; border-radius: 3px !important;
            background: #fff !important; border: 1px solid var(--inv-bd) !important;
            box-shadow: none !important; font-size: 11px !important;
          }
          .wh-stats { padding: 5px 0; background: #f8fafc; }
          .wh-label { font-size: 9px !important; }
          .wh-val   { font-size: 13px !important; }
          .wh-stat-col.highlight .wh-label { opacity: .85; }
          .wh-stat-col.highlight .wh-val   { font-size: 1rem !important; }
          #forecastDetailsModal .modal-dialog,
          #locationDetailsModal .modal-dialog { margin: 8px !important; }
        }
      </style>

      <main id="wrap" class="oe_structure">
        <t t-if="not pw_ok">
          <section>
            <div class="container mt-5 mb-6" style="max-width:560px;">
              <h1 class="mb-3 text-center">Tra cứu tồn kho</h1>
              <t t-if="pw_err"><div class="alert alert-danger">Mật khẩu không đúng. Vui lòng <strong>tải lại trang (F5)</strong>.</div></t>
              <t t-else="">
                <form method="post" t-att-action="request.httprequest.path" class="card p-4 shadow-sm border-0">
                  <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                  <label class="form-label fw-bold">Nhập mật khẩu truy cập</label>
                  <input type="password" name="inv_password" class="form-control mb-3 form-control-lg" autocomplete="off" required="required"/>
                  <button type="submit" class="btn btn-primary w-100 btn-lg">Xác nhận</button>
                </form>
              </t>
            </div>
          </section>
        </t>

        <t t-if="pw_ok">
          <section>
            <div class="container-fluid mt-3 mb-6">
              <!-- Navigation Tabs -->
              <div>
                <ul class="nav nav-pills justify-content-center flex-wrap gap-2">
                  <li class="nav-item">
                    <a class="nav-link active bg-primary text-white px-3 py-2" href="/search_stock" style="font-weight:600;font-size:.9rem;">
                      <i class="fa fa-cubes me-1"></i><span>Tồn kho</span>
                    </a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link text-dark px-3 py-2" href="/misa/purchase/lookup" style="font-weight:500;font-size:.9rem;background:#f8f9fa;">
                      <i class="fa fa-shopping-cart me-1"></i><span>Chứng từ mua</span>
                    </a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link text-dark px-3 py-2" href="/search_invoice" style="font-weight:500;font-size:.9rem;background:#f8f9fa;">
                      <i class="fa fa-file-text me-1"></i><span>Hóa đơn MISA</span>
                    </a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link text-dark px-3 py-2" href="/sale_plan" style="font-weight:500;font-size:.9rem;background:#f8f9fa;">
                      <i class="fa fa-truck me-1"></i><span>Tình trạng đơn</span>
                    </a>
                  </li>
                </ul>
              </div>
              
              <form t-att-action="request.httprequest.path" method="get" class="row g-2 mb-4" id="search-form">
                <div class="col-md-5 position-relative">
                    <input type="hidden" name="q" id="real-search-q" t-att-value="q"/>
                    <div class="search-wrapper" id="chip-container">
                        <input type="text" id="visual-search-input" class="search-input-field" placeholder="Tìm tên, mã, barcode..." autocomplete="off"/>
                    </div>
                    <div id="search-suggestions" class="position-absolute w-100 bg-white overflow-auto" style="display:none; z-index:1050; top:100%; left:0; margin-top:4px;"></div>
                </div>
                <div class="col-md-2">
                  <select name="warehouse_id" class="form-select"> <option t-att-selected="not warehouse_id" value="">Tất cả kho</option>
                    <t t-foreach="warehouses" t-as="w">
                      <option t-att-value="w.id" t-att-selected="int(warehouse_id)==w.id if warehouse_id else False"><t t-esc="w.name"/></option>
                    </t>
                  </select>
                </div>
                <div class="col-md-3 d-flex align-items-center justify-content-center gap-3">
                  <div class="form-check form-switch pt-1">
                    <input class="form-check-input" type="checkbox" id="comboSearchCheck" name="combo_search" value="1" t-att-checked="request.params.get('combo_search') in ('1','true','on')"/>
                    <label class="form-check-label user-select-none" for="comboSearchCheck">Tìm Combo</label>
                  </div>
                  <div class="form-check form-switch pt-1">
                    <input class="form-check-input" type="checkbox" id="lowStockCheck" name="low_stock" value="1" t-att-checked="request.params.get('low_stock') in ('1','true','on')"/>
                    <label class="form-check-label user-select-none" for="lowStockCheck">Tồn ≤ 5</label>
                  </div>
                </div>
                <div class="col-md-2">
                  <button class="btn btn-primary w-100 fw-bold" type="submit"><i class="fa fa-search me-1"></i> Tìm kiếm</button>
                </div>
              </form>

              <t t-if="rows">
                <div class="card shadow-sm border-0 bg-transparent">
                    <div class="table-responsive">
                      <table class="table table-sm table-hover align-middle mb-0" id="inv-table">
                        <thead class="bg-light">
                          <tr>
                            <th style="width:40px;"></th>
                            <th style="width:70px;">Ảnh</th>
                            <th>Mã</th>
                            <th>Sản phẩm</th>
                            <th>Barcode</th>
                            <th>ĐVT</th>
                            <th class="text-end" style="width:80px">Dự báo</th>
                            <th class="text-end fw-bold" style="width:80px">Thực tế</th>
                            <th class="text-end">Giá Web</th>
                            <th class="text-end">Giá TMDT</th>
                            <th class="text-end">Niêm yết</th>
                            <th class="text-end">Giá TM</th>
                            <th class="text-end">Giá bán</th>
                            <th style="width:50px;"></th>
                          </tr>
                        </thead>
                        <tbody>
                          <t t-foreach="rows" t-as="r">
                            <t t-set="is_combo" t-value="r.get('is_combo', False)"/>
                            <tr t-att-id="'row-'+str(r['id'])" class="main-row">
                              <td>
                                <button type="button" class="btn btn-sm btn-light text-primary btn-expand rounded-circle"
                                        t-att-data-pid="r['id']"
                                        t-att-data-is-combo="str(bool(r.get('is_combo'))).lower()">
                                  <i class="fa fa-plus"></i>
                                </button>
                              </td>
                              <td>
                                <div class="position-relative" style="width: 50px; height: 50px;">
                                    <img t-att-src="r['image_url']" class="" style="width:100%; height:100%; object-fit:cover;"/>
                                </div>
                              </td>
                              <td class="fw-bold text-primary"><t t-esc="r['default_code']"/></td>
                              <td>
                                <div class="text-wrap" style="min-width: 200px;">
                                    <t t-esc="r['name']"/>
                                    <t t-if="is_combo"><span class="badge bg-warning text-dark ms-1">COMBO</span></t>
                                    <div class="d-md-none text-muted small mt-1">Mã: <t t-esc="r['default_code']"/></div>
                                </div>
                              </td>
                              <td class="text-muted"><small><t t-esc="r['barcode']"/></small></td>
                              <td><t t-esc="r['uom']"/></td>
                              <td class="text-end text-muted">
                                <t t-if="is_combo">-</t>
                                <t t-else="">
                                  <a href="#" class="text-muted text-decoration-none btn-forecast-detail"
                                     t-att-data-pid="r['id']"
                                     t-att-data-name="r['name']"
                                     title="Xem chi tiết dự báo">
                                    <t t-esc="'{:,.0f}'.format(r.get('qty_forecasted', 0))"/>
                                  </a>
                                </t>
                              </td>
                              <td class="text-end fw-bold fs-6">
                                <t t-if="is_combo">-</t>
                                <t t-else="">
                                    <span t-att-class="'text-danger' if r.get('qty_total', 0) &lt;= 5 else 'text-success'">
                                        <t t-esc="'{:,.0f}'.format(r.get('qty_total', 0))"/>
                                    </span>
                                </t>
                              </td>
                              <td class="text-end"><t t-if="r.get('price_web')"><t t-esc="'{:,.0f}'.format(r['price_web'])"/></t></td>
                              <td class="text-end"><t t-if="r.get('price_tmdt')"><t t-esc="'{:,.0f}'.format(r['price_tmdt'])"/></t></td>
                              <td class="text-end"><t t-if="r.get('price_listed')"><t t-esc="'{:,.0f}'.format(r['price_listed'])"/></t></td>
                              <td class="text-end"><t t-if="r.get('commercial_price')"><t t-esc="'{:,.0f}'.format(r['commercial_price'])"/></t></td>
                              <td class="text-end"><t t-if="r.get('list_price')"><t t-esc="'{:,.0f}'.format(r['list_price'])"/></t></td>
                              <td>
                                <t t-if="r['website_url']"><a t-att-href="r['website_url']" target="_blank" class="text-decoration-none"><i class="fa fa-external-link"></i></a></t>
                              </td>
                            </tr>
                            <!-- DETAIL ROW (Server-Side Rendered) -->
                            <tr class="detail-row">
                                <td colspan="14" style="padding:0; border:none;">
                                    <t t-set="bk" t-value="r.get('breakdown', {})"/>
                                    <!-- COMBO LAYOUT -->
                                    <t t-if="is_combo">
                                        <div class="p-3 detail-container-mobile">
                                            <div class="row g-3">
                                                <t t-foreach="bk.get('rows', [])" t-as="c">
                                                    <div class="col-12 col-md-12">
                                                        <div class="card h-100 shadow-sm">
                                                            <div class="card-header bg-white d-flex align-items-center py-2">
                                                                <img t-att-src="c['image_url']" class="me-3" style="width:40px; height:40px; object-fit:contain;"/>
                                                                <div class="flex-grow-1">
                                                                    <div class="fw-bold text-primary"><t t-esc="c['name']"/></div>
                                                                    <div class="small text-muted">SL Combo: <b><t t-esc="'{:,.0f}'.format(c['component_qty_in_combo'])"/></b></div>
                                                                </div>
                                                            </div>
                                                            <div class="card-body p-0">
                                                                <t t-foreach="c.get('warehouses', [])" t-as="w">
                                                                    <div class="border-bottom p-2">
                                                                        <div class="fw-bold small mb-1 d-flex justify-content-between align-items-center">
                                                                            <span><t t-esc="w['warehouse_name']"/></span>
                                                                            <button type="button" class="btn btn-sm btn-outline-primary py-0 px-2 btn-view-location"
                                                                                    t-att-data-pid="c['child_product_id']" t-att-data-wid="w['warehouse_id']" t-att-data-name="c['name']">
                                                                                <i class="fa fa-map-marker"></i> Vị trí
                                                                            </button>
                                                                        </div>
                                                                        <div class="d-flex">
                                                                            <div class="flex-fill text-center border-end">
                                                                                <div class="small text-muted">Sẵn sàng</div>
                                                                                <div class="fw-bold text-success"><t t-esc="'{:,.0f}'.format(w['qty_available'])"/></div>
                                                                            </div>
                                                                            <div class="flex-fill text-center">
                                                                                <div class="small text-muted">Thực tế</div>
                                                                                <div><t t-esc="'{:,.0f}'.format(w['qty_total'])"/></div>
                                                                            </div>
                                                                        </div>
                                                                    </div>
                                                                </t>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </t>
                                            </div>
                                        </div>
                                    </t>
                                    
                                    <!-- REGULAR LAYOUT -->
                                    <t t-else="">
                                        <div class="detail-container-mobile">
                                            <!-- Desktop -->
                                            <div class="d-none d-md-block bg-white">
                                                <table class="table table-sm mb-0 shadow-sm border">
                                                    <thead class="table-light">
                                                        <tr>
                                                            <th class="py-2 ps-3 text-muted fst-italic">Kho hàng</th>
                                                            <th class="py-2 text-end text-muted fst-italic">Tồn</th>
                                                            <th class="py-2 text-end text-muted fst-italic">Giữ</th>
                                                            <th class="py-2 text-end text-muted fst-italic pe-3">Sẵn sàng</th>
                                                            <th class="py-2 text-center text-muted fst-italic" style="width: 80px;">Vị trí</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        <t t-foreach="bk.get('rows', [])" t-as="w">
                                                            <tr>
                                                                <td class="ps-3"><t t-esc="w['warehouse_name']"/></td>
                                                                <td class="text-end"><t t-esc="'{:,.0f}'.format(w['qty_total'])"/></td>
                                                                <td class="text-end"><t t-esc="'{:,.0f}'.format(w['qty_reserved'])"/></td>
                                                                <td t-attf-class="text-end pe-3 #{ 'text-success fw-bold' if w['qty_available'] > 0 else 'text-muted' }">
                                                                    <t t-esc="'{:,.0f}'.format(w['qty_available'])"/>
                                                                </td>
                                                                <td class="text-center">
                                                                    <button type="button" class="btn btn-sm btn-light text-primary btn-view-location border py-0 px-2"
                                                                            t-att-data-pid="r['id']" t-att-data-wid="w['warehouse_id']" t-att-data-name="r['name']" title="Xem chi tiết vị trí">
                                                                        <i class="fa fa-map-marker"></i>
                                                                    </button>
                                                                </td>
                                                            </tr>
                                                        </t>
                                                    </tbody>
                                                </table>
                                            </div>
                                            <!-- Mobile -->
                                            <div class="d-md-none">
                                                <t t-foreach="bk.get('rows', [])" t-as="w">
                                                     <div class="wh-card">
                                                       <div class="wh-header d-flex justify-content-between align-items-center">
                                                           <span><t t-esc="w['warehouse_name']"/></span>
                                                           <button type="button" class="btn btn-sm btn-outline-primary py-0 px-2 btn-view-location"
                                                                   t-att-data-pid="r['id']" t-att-data-wid="w['warehouse_id']" t-att-data-name="r['name']">
                                                               <i class="fa fa-map-marker"></i> Vị trí
                                                           </button>
                                                       </div>
                                                       <div class="wh-stats">
                                                         <div t-attf-class="wh-stat-col #{ 'dim' if w['qty_total']==0 else '' }"><div class="wh-label">Tổng</div><div class="wh-val"><t t-esc="'{:,.0f}'.format(w['qty_total'])"/></div></div>
                                                         <div t-attf-class="wh-stat-col #{ 'dim' if w['qty_reserved']==0 else '' }"><div class="wh-label">Đã giữ</div><div class="wh-val text-muted"><t t-esc="'{:,.0f}'.format(w['qty_reserved'])"/></div></div>
                                                         <div class="wh-stat-col highlight"><div class="wh-label">Có thể bán</div><div class="wh-val"><t t-esc="'{:,.0f}'.format(w['qty_available'])"/></div></div>
                                                       </div>
                                                     </div>
                                                </t>
                                            </div>
                                        </div>
                                    </t>
                                </td>
                            </tr>
                          </t>
                        </tbody>
                      </table>
                    </div>
                </div>
                <div class="d-flex justify-content-center mt-4">
                    <nav t-if="pages &gt; 1">
                        <ul class="pagination pagination-lg">
                            <li t-attf-class="page-item #{'disabled' if page &lt;= 1 else ''}">
                                <a class="page-link" t-att-href="request.httprequest.path + ('?q=%s&amp;warehouse_id=%s&amp;low_stock=%s&amp;combo_search=%s&amp;page=%d' % (q, warehouse_id or '', '1' if request.params.get('low_stock') else '', '1' if request.params.get('combo_search') else '', page-1))"><i class="fa fa-chevron-left"></i></a>
                            </li>
                            <li class="page-item active"><span class="page-link">Trang <t t-esc="page"/> / <t t-esc="pages"/></span></li>
                            <li t-attf-class="page-item #{'disabled' if page &gt;= pages else ''}">
                                <a class="page-link" t-att-href="request.httprequest.path + ('?q=%s&amp;warehouse_id=%s&amp;low_stock=%s&amp;combo_search=%s&amp;page=%d' % (q, warehouse_id or '', '1' if request.params.get('low_stock') else '', '1' if request.params.get('combo_search') else '', page+1))"><i class="fa fa-chevron-right"></i></a>
                            </li>
                        </ul>
                    </nav>
                </div>
              </t>
              <t t-else="">
                <div class="text-center py-5">
                    <img src="/web/static/img/smiling_face.svg" style="width:64px; opacity:0.5;"/>
                    <p class="mt-3 text-muted">Không tìm thấy sản phẩm nào phù hợp.</p>
                </div>
              </t>
            </div>

            <!-- Modal Forecast Details -->
            <div class="modal fade" id="forecastDetailsModal" tabindex="-1" aria-hidden="true" style="z-index:1060;">
              <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable" style="max-width:min(1100px,96vw);width:100%;margin:auto;">
                <div class="modal-content" style="border:none; max-width : 100%!important;border-radius:16px;box-shadow:0 24px 64px rgba(0,0,0,.18);overflow:hidden;">
                  <div class="modal-header" style="background:#fff;border-bottom:1px solid #e5e7eb;padding:18px 24px 14px;align-items:flex-start;">
                    <div>
                      <div style="display:flex;align-items:center;gap:9px;">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#6366f1" stroke-width="2.5" style="flex-shrink:0;"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
                        <span style="font-size:1rem;font-weight:700;color:#111827;">Chi tiết dự báo tồn kho</span>
                      </div>
                      <div id="forecastDetailsProductName" style="font-size:.82rem;color:#6b7280;margin-top:5px;padding-left:27px;font-weight:500;"></div>
                    </div>
                    <button type="button" class="btn-close ms-auto" data-bs-dismiss="modal" aria-label="Close"></button>
                  </div>
                  <div class="modal-body p-0" id="forecastDetailsBody">
                    <div style="display:flex;flex-direction:column;align-items:center;justify-content:center;padding:56px 24px;gap:14px;">
                      <div class="spinner-border" style="color:#6366f1;" role="status"><span class="visually-hidden">Loading...</span></div>
                      <div style="color:#9ca3af;font-size:.85rem;">Đang tải...</div>
                    </div>
                  </div>
                  <div class="modal-footer" style="border-top:1px solid #e5e7eb;padding:12px 24px;background:#fafafa;">
                    <button type="button" class="btn btn-sm" style="background:#f3f4f6;border:1px solid #e5e7eb;color:#374151;padding:6px 20px;border-radius:8px;font-weight:600;" data-bs-dismiss="modal">Đóng</button>
                  </div>
                </div>
              </div>
            </div>

            <!-- Modal Location Details -->
            <div class="modal fade" id="locationDetailsModal" tabindex="-1" aria-labelledby="locationDetailsModalLabel" aria-hidden="true" style="z-index: 1060;">
              <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
                <div class="modal-content">
                  <div class="modal-header">
                    <h5 class="modal-title" id="locationDetailsModalLabel">Chi tiết vị trí tồn kho</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                  </div>
                  <div class="modal-body p-0">
                    <div id="locationDetailsProductName" class="bg-light p-2 border-bottom fw-bold text-center text-primary"></div>
                    <div id="locationDetailsBody" class="p-3">
                        <div class="text-center py-3">
                            <div class="spinner-border text-primary" role="status">
                                <span class="visually-hidden">Loading...</span>
                            </div>
                        </div>
                    </div>
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Đóng</button>
                  </div>
                </div>
              </div>
            </div>

          </section>

          <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
          
       <script type="text/javascript"><![CDATA[
            (function () {
              var searchTimeout = null;
              // ... INIT CHIP ...
              function initChipInput() {
                  var $realInput = $('#real-search-q');
                  var $container = $('#chip-container');
                  var $visualInput = $('#visual-search-input');
                  var currentChips = [];
                  var initialVal = $realInput.val();
                  if(initialVal) {
                      currentChips = initialVal.split(',').map(function(s){ return s.trim(); }).filter(function(s){ return s; });
                      renderChips();
                  }
                  function renderChips() {
                      $container.find('.search-chip').remove();
                      currentChips.forEach(function(term, index) {
                          var $chip = $('<div class="search-chip"><span>' + $('<div>').text(term).html() + '</span><span class="btn-close-chip" data-index="' + index + '">&times;</span></div>');
                          $visualInput.before($chip);
                      });
                      updateRealInput();
                  }
                  function updateRealInput() { $realInput.val(currentChips.join(', ')); }
                  window.addSearchChip = function(term) {
                      term = term.trim().replace(/,/g, '');
                      if(term && !currentChips.includes(term)) { currentChips.push(term); renderChips(); }
                      $visualInput.val(''); $visualInput.focus();
                  }
                  $container.on('click', '.btn-close-chip', function() {
                      currentChips.splice($(this).data('index'), 1); renderChips(); $visualInput.focus();
                  });
                  $visualInput.on('keydown', function(e) {
                      var val = $(this).val();
                      if(e.key === 'Backspace' && val === '' && currentChips.length > 0) { currentChips.pop(); renderChips(); }
                      else if (e.key === 'Enter') { e.preventDefault(); if (val.trim()) addSearchChip(val); $('#search-form').submit(); }
                      else if (e.key === ',') { e.preventDefault(); if (val.trim()) addSearchChip(val); }
                  });
                  $container.on('click', function(e) { if(e.target === this) $visualInput.focus(); });
              }

              // ... DEBOUNCE & SEARCH AJAX ...
              function debounce(func, wait) {
                return function() { clearTimeout(searchTimeout); var context=this, args=arguments; searchTimeout = setTimeout(function() { func.apply(context, args); }, wait); };
              }
              function searchProducts(term, callback) {
                var isComboSearch = $('#comboSearchCheck').is(':checked');
                $.ajax({
                  url: "/search_stock/suggest",
                  type: "POST", dataType: "json", contentType: "application/json",
                  data: JSON.stringify({ jsonrpc: "2.0", method: "call", params: { q: term, combo_search: isComboSearch }, id: new Date().getTime() }),
                  success: function(resp) { callback((resp && resp.result && resp.result.products) ? resp.result.products : []); }
                });
              }
              function renderSuggestions(products) {
                var $dropdown = $('#search-suggestions');
                if (!products || products.length === 0) { $dropdown.hide().empty(); return; }
                var html = '';
                products.forEach(function(p) {
                   var displayName = p.name || p.default_code || '';
                   var imgUrl = p.image_url || '/web/static/img/placeholder.png';
                   var q = p.qty_total || 0;
                   var badgeClass = 'bg-secondary';
                   if (q > 10) badgeClass = 'bg-success';
                   else if (q > 5) badgeClass = 'bg-info';
                   else if (q > 0) badgeClass = 'bg-warning text-dark';
                   
                   var qtyInfo = (q > 0) ? ('<span class="badge ' + badgeClass + ' text-white border ms-2">Tồn: ' + fmt(q) + '</span>') : '<span class="badge bg-light text-muted border ms-2">Hết hàng</span>';
                   if (q > 0 && q <= 5) qtyInfo = '<span class="badge ' + badgeClass + ' border ms-2">Tồn: ' + fmt(q) + '</span>';
                   
                   var searchTerm = displayName;
                   html += '<div class="suggestion-item border-bottom p-2" data-name="' + displayName + '" data-code="' + (p.default_code || '') + '" data-search-term="' + searchTerm + '" style="cursor:pointer;">';
                   html += '  <div class="d-flex align-items-center">';
                   html += '    <img src="' + imgUrl + '" class=" me-2 flex-shrink-0" style="width:40px; height:40px; object-fit:cover;"/>';
                   html += '    <div class="flex-grow-1 overflow-hidden">';
                   html += '      <div class="d-flex align-items-center mb-1">';
                   html += '        <div class="fw-bold text-dark text-truncate" style="font-size:0.9rem;">' + (p.default_code || '') + '</div>';
                   html +=          qtyInfo;
                   html += '      </div>';
                   html += '      <div class="text-muted text-truncate small">' + (p.name || '') + '</div>';
                   html += '    </div>';
                   html += '    <div class="ms-2 text-primary"><i class="fa fa-angle-right"></i></div>';
                   html += '  </div></div>';
                });
                $dropdown.html(html).show();
              }

              // --- HÀM TÁCH RIÊNG: CHỊU TRÁCH NHIỆM TẢI AJAX ---
              // Hàm này chỉ tải và hiển thị, KHÔNG đóng lại
              function loadDetailRow($btn) {
                   var pid = $btn.data('pid');
                   var isCombo = String($btn.data('is-combo')) === 'true';
                   var $row = $btn.closest('tr'); 
                   
                   // Kiểm tra nếu đã có detail-row rồi thì thôi, không tải nữa (tránh trùng)
                   var $next = $row.next();
                   if ($next.length && $next.hasClass('detail-row')) return;

                   var colCount = $row.children('td').length;
                   var $detail = $('<tr class="detail-row"><td colspan="' + colCount + '"><div class="p-3 bg-light text-center"><span class="spinner-border spinner-border-sm me-2"></span>Đang tải...</div></td></tr>');
                   $row.after($detail);
                   $btn.html('<i class="fa fa-minus"></i>');

                   var currentWid = $('select[name="warehouse_id"]').val() || '';
                   var detailMode = isCombo ? 'per_warehouse' : null;

                   $.ajax({
                      url: "/search_stock/product_breakdown",
                      type: "POST", dataType: "json", contentType: "application/json",
                      data: JSON.stringify({jsonrpc: "2.0", method: "call", params: { product_id: pid, warehouse_id: currentWid, detail_mode: detailMode }, id: 1}),
                      success: function(resp) {
                          var res = (resp && resp.result) ? resp.result : {};
                          if(res.ok) {
                              $detail.find('td').html(buildDetailTable(res));
                          } else {
                              $detail.find('td').html('<div class="text-danger small p-3">Lỗi: ' + (res.error || 'Unknown') + '</div>');
                          }
                      }
                   });
              }

              $(document).ready(function() {
                initChipInput(); 
                var $visualInput = $('#visual-search-input');
                var $dropdown = $('#search-suggestions');
                $visualInput.on('input', debounce(function() {
                  var term = $(this).val().trim();
                  if (term.length >= 2) searchProducts(term, renderSuggestions);
                  else $dropdown.hide();
                }, 300));
                $(document).on('click', '.suggestion-item', function() {
                  var term = $(this).data('name') || $(this).data('search-term');
                  if (term) { window.addSearchChip(term); $dropdown.hide(); $('#search-form').submit(); }
                });
                $(document).on('click', function(e) { if (!$(e.target).closest('#chip-container, #search-suggestions').length) $dropdown.hide(); });
                
                // --- SỰ KIỆN CLICK (THỦ CÔNG) ---
                // Khi bấm tay thì giữ logic Toggle: Đang mở -> Đóng, Đang đóng -> Mở
                $(document).on('click', '.btn-expand', function () {
                   var $btn = $(this);
                   var $row = $btn.closest('tr');
                   var $next = $row.next();

                   if ($next.is(':visible')) { 
                       $next.hide(); 
                       $btn.html('<i class="fa fa-plus"></i>'); 
                   } else {
                       $next.show();
                       $btn.html('<i class="fa fa-minus"></i>');
                   }
                });

                // --- VIEW LOCATION DETAILS ---
                $(document).on('click', '.btn-view-location', function(e) {
                    e.preventDefault();
                    var pid = $(this).data('pid');
                    var wid = $(this).data('wid');
                    var name = $(this).data('name');
                    
                    $('#locationDetailsProductName').text(name);
                    $('#locationDetailsBody').html('<div class="text-center py-4"><div class="spinner-border text-primary" role="status"></div><div class="mt-2 text-muted">Đang tải...</div></div>');
                    
                    var myModalElem = document.getElementById('locationDetailsModal');
                    // Check if Bootstrap 5 exists, if not, use minimal approach
                    if (typeof bootstrap !== 'undefined' && bootstrap.Modal) {
                        var myModal = bootstrap.Modal.getInstance(myModalElem) || new bootstrap.Modal(myModalElem);
                        myModal.show();
                    } else {
                        // Fallback if bootstrap JS is not perfectly loaded
                        $(myModalElem).show().addClass('show');
                        $(document.body).append('<div class="modal-backdrop fade show" id="customBackdrop"></div>');
                        $(myModalElem).find('.btn-close, .btn-secondary').off('click').on('click', function() {
                            $(myModalElem).hide().removeClass('show');
                            $('#customBackdrop').remove();
                        });
                    }
                    
                    $.ajax({
                        url: "/search_stock/location_details",
                        type: "POST", dataType: "json", contentType: "application/json",
                        data: JSON.stringify({ jsonrpc: "2.0", method: "call", params: { product_id: pid, warehouse_id: wid }, id: new Date().getTime() }),
                        success: function(resp) {
                            var data = resp.result;
                            if (data && data.ok) {
                                if (data.locations && data.locations.length > 0) {
                                    var html = '<table class="table table-sm table-bordered mb-0">';
                                    html += '<thead class="table-light"><tr><th class="text-center">Vị trí</th><th class="text-center">Tồn</th><th class="text-center">Sẵn sàng</th></tr></thead><tbody>';
                                    data.locations.forEach(function(loc) {
                                        html += '<tr>';
                                        html += '<td class="text-center">' + loc.location + '</td>';
                                        html += '<td class="text-center fw-bold">' + fmt(loc.qty) + '</td>';
                                        html += '<td class="text-center text-success">' + fmt(loc.available) + '</td>';
                                        html += '</tr>';
                                    });
                                    html += '</tbody></table>';
                                    $('#locationDetailsBody').html(html);
                                } else {
                                    $('#locationDetailsBody').html('<div class="alert alert-warning mb-0 text-center">Không có tồn kho tại các vị trí trong kho này.</div>');
                                }
                            } else {
                                $('#locationDetailsBody').html('<div class="alert alert-danger mb-0 text-center">Lỗi: ' + (data ? data.error : 'Không thể tải dữ liệu') + '</div>');
                            }
                        },
                        error: function() {
                            $('#locationDetailsBody').html('<div class="alert alert-danger mb-0 text-center">Lỗi kết nối. Vui lòng thử lại.</div>');
                        }
                    });
                });
              });
              
              function fmt(n) { return (parseFloat(n)||0).toLocaleString('en-US'); }
              
              function buildDetailTable(data) {
                  var html = '';
                  var rows = data.rows || [];

                  // CASE 1: COMBO
                  if(data.mode === 'components_by_warehouse') {
                      html += '<div class="p-3 detail-container-mobile"><div class="row g-3">';
                      rows.forEach(function(c){
                          var imgUrl = c.image_url || '/web/static/img/placeholder.png';
                          html += '<div class="col-12 col-md-12">';
                          html += '  <div class="card h-100 border shadow-sm">';
                          html += '    <div class="card-header bg-white d-flex align-items-center py-2">';
                          html += '      <img src="' + imgUrl + '" class=" me-3" style="width:40px; height:40px; object-fit:contain;">';
                          html += '      <div class="flex-grow-1">';
                          html += '        <div class="fw-bold text-primary">' + c.name + '</div>';
                          html += '        <div class="small text-muted">SL Combo: <b>' + fmt(c.component_qty_in_combo) + '</b></div>';
                          html += '      </div>';
                          html += '    </div>';
                          html += '    <div class="card-body p-0">';
                          (c.warehouses||[]).forEach(function(w){
                              html += '<div class="border-bottom p-2">';
                              html += '  <div class="fw-bold small mb-1">' + w.warehouse_name + '</div>';
                              html += '  <div class="d-flex">';
                              html += '    <div class="flex-fill text-center border-end"><div class="small text-muted">Sẵn sàng</div><div class="fw-bold text-success">' + fmt(w.qty_available) + '</div></div>';
                              html += '    <div class="flex-fill text-center"><div class="small text-muted">Thực tế</div><div>' + fmt(w.qty_total) + '</div></div>';
                              html += '  </div>';
                              html += '</div>';
                          });
                          html += '    </div></div></div>';
                      });
                      html += '</div></div>';
                  } 
                  
                  // CASE 2: SẢN PHẨM THƯỜNG
                  else {
                      html += '<div class="detail-container-mobile">';
                      html += '<div class="d-none d-md-block p-3 bg-white">';
                      html += '<table class="table table-sm mb-0"><thead><tr><th>Kho hàng</th><th class="text-end">Tồn</th><th class="text-end">Giữ</th><th class="text-end">Sẵn sàng</th></tr></thead><tbody>';
                      rows.forEach(function(r){
                          var availClass = (r.qty_available > 0) ? 'text-success fw-bold' : 'text-muted';
                          html += '<tr><td>' + r.warehouse_name + '</td><td class="text-end">' + fmt(r.qty_total) + '</td><td class="text-end">' + fmt(r.qty_reserved) + '</td><td class="text-end ' + availClass + '">' + fmt(r.qty_available) + '</td></tr>';
                      });
                      html += '</tbody></table></div>';

                      html += '<div class="d-md-none">';
                      rows.forEach(function(r){
                           html += '<div class="wh-card">';
                           html += '  <div class="wh-header">' + r.warehouse_name + '</div>';
                           html += '  <div class="wh-stats">';
                           html += '    <div class="wh-stat-col ' + (r.qty_total==0 ? 'dim':'') + '"><div class="wh-label">Tổng</div><div class="wh-val">' + fmt(r.qty_total) + '</div></div>';
                           html += '    <div class="wh-stat-col ' + (r.qty_reserved==0 ? 'dim':'') + '"><div class="wh-label">Đã giữ</div><div class="wh-val text-muted">' + fmt(r.qty_reserved) + '</div></div>';
                           html += '    <div class="wh-stat-col highlight"><div class="wh-label">Có thể bán</div><div class="wh-val">' + fmt(r.qty_available) + '</div></div>';
                           html += '  </div>';
                           html += '</div>';
                      });
                      html += '</div>';
                      html += '</div>';
                  }
                  
                  return html;
              }

              // --- FORECAST DETAILS ---
              $(document).on('click', '.btn-forecast-detail', function(e) {
                  e.preventDefault();
                  var pid = $(this).data('pid');
                  var name = $(this).data('name');
                  var wid = $('select[name="warehouse_id"]').val() || '';

                  $('#forecastDetailsProductName').text(name);
                  $('#forecastDetailsBody').html(
                      '<div style="display:flex;flex-direction:column;align-items:center;justify-content:center;padding:56px 24px;gap:14px;">'+
                      '<div class="spinner-border" style="color:#6366f1;" role="status"></div>'+
                      '<div style="color:#9ca3af;font-size:.85rem;">Đang tải...</div></div>'
                  );

                  var modalElem = document.getElementById('forecastDetailsModal');
                  if (typeof bootstrap !== 'undefined' && bootstrap.Modal) {
                      var modal = bootstrap.Modal.getInstance(modalElem) || new bootstrap.Modal(modalElem);
                      modal.show();
                  } else {
                      $(modalElem).show().addClass('show');
                      $('body').append('<div class="modal-backdrop fade show" id="forecastBackdrop"></div>');
                      $(modalElem).find('[data-bs-dismiss="modal"]').off('click.forecast').on('click.forecast', function() {
                          $(modalElem).hide().removeClass('show');
                          $('#forecastBackdrop').remove();
                      });
                  }

                  $.ajax({
                      url: "/search_stock/forecast_details",
                      type: "POST", dataType: "json", contentType: "application/json",
                      data: JSON.stringify({ jsonrpc: "2.0", method: "call", params: { product_id: pid, warehouse_id: wid }, id: new Date().getTime() }),
                      success: function(resp) {
                          var data = resp && resp.result;
                          if (!data || !data.ok) {
                              $('#forecastDetailsBody').html('<div class="alert alert-danger m-2">Lỗi: ' + (data ? data.error : 'Không tải được dữ liệu') + '</div>');
                              return;
                          }
                          $('#forecastDetailsBody').html(buildForecastHtml(data));
                      },
                      error: function() {
                          $('#forecastDetailsBody').html('<div class="alert alert-danger m-2">Lỗi kết nối. Vui lòng thử lại.</div>');
                      }
                  });
              });

              function _stateStyle(state) {
                  var m = {
                      'Sẵn sàng': {bg:'#f0fdf4',color:'#16a34a',border:'#86efac'},
                      'Xác nhận': {bg:'#eff6ff',color:'#2563eb',border:'#93c5fd'},
                      'Đang chờ': {bg:'#fffbeb',color:'#b45309',border:'#fcd34d'},
                      'Hoàn tất': {bg:'#f3f4f6',color:'#6b7280',border:'#d1d5db'},
                      'Đã hủy':   {bg:'#fef2f2',color:'#dc2626',border:'#fca5a5'},
                  };
                  return m[state] || {bg:'#eff6ff',color:'#2563eb',border:'#93c5fd'};
              }
              function _metaCell(label, val, color, bold) {
                  return '<div>'
                      + '<div style="font-size:.66rem;font-weight:700;text-transform:uppercase;letter-spacing:.6px;color:#9ca3af;margin-bottom:3px;">'+label+'</div>'
                      + '<div style="font-size:.85rem;color:'+(color||'#374151')+';font-weight:'+(bold?'700':'500')+';word-break:break-word;">'+val+'</div>'
                      + '</div>';
              }

              // Dùng CSS class để responsive: card trên mobile, table trên desktop
              var _fcss = '<style id="fc-style">';
              _fcss += '.fc-table{width:100%;border-collapse:collapse;font-size:.82rem;}';
              _fcss += '.fc-table th{background:#f9fafb;padding:8px 12px;font-size:.68rem;font-weight:700;text-transform:uppercase;letter-spacing:.5px;color:#9ca3af;border-bottom:2px solid #e5e7eb;white-space:nowrap;}';
              _fcss += '.fc-table td{padding:10px 12px;border-bottom:1px solid #f3f4f6;vertical-align:middle;}';
              _fcss += '.fc-table tr:last-child td{border-bottom:none;}';
              _fcss += '.fc-table tr:hover td{background:#fafafa;}';
              _fcss += '.fc-table .fc-num{font-weight:600;font-size:.95rem;}';
              _fcss += '.fc-table .fc-pick{font-weight:700;color:#374151;}';
              _fcss += '.fc-badge{display:inline-flex;align-items:center;padding:2px 8px;border-radius:5px;font-size:.68rem;font-weight:700;border-width:1px;border-style:solid;}';
              _fcss += '@media(max-width:640px){.fc-desktop{display:none!important;}.fc-mobile{display:block!important;}}';
              _fcss += '@media(min-width:641px){.fc-mobile{display:none!important;}.fc-desktop{display:block!important;}}';
              _fcss += '</style>';

              function buildForecastHtml(data) {
                  var html = _fcss;
                  var isPos = data.qty_forecast >= 0;
                  var fqColor = isPos ? '#16a34a' : '#dc2626';

                  // === FORMULA ROW ===
                  var fqBg   = isPos ? '#f0fdf4' : '#fef2f2';
                  var fqBord = isPos ? '#4ade80' : '#f87171';
                  var fqSh   = isPos ? 'rgba(22,163,74,.18)' : 'rgba(220,38,38,.18)';
                  html += '<div style="padding:20px 28px 18px;border-bottom:2px solid #f3f4f6;background:#fafafa;">';
                  html += '<div style="display:flex;align-items:center;justify-content:center;gap:12px;flex-wrap:wrap;">';
                  html += _pill('Tồn kho',   fmt(data.qty_on_hand),    '#fff',   '#e5e7eb',  '#111827', '#9ca3af');
                  html += '<span style="font-size:24px;font-weight:700;color:#16a34a;">+</span>';
                  html += _pill('Nhập ĐMH', '+'+fmt(data.total_incoming), '#f0fdf4','#86efac','#16a34a','#16a34a');
                  html += '<span style="font-size:24px;font-weight:700;color:#dc2626;">−</span>';
                  html += _pill('Xuất ĐBH', '−'+fmt(data.total_outgoing), '#fef2f2','#fca5a5','#dc2626','#dc2626');
                  html += '<span style="font-size:24px;font-weight:700;color:#6b7280;">=</span>';
                  html += _pill('Dự báo',   fmt(data.qty_forecast),    fqBg, fqBord, fqColor, fqColor, true, fqSh);
                  html += '</div></div>';

                  var inCount  = data.incoming.length;
                  var outCount = data.outgoing.length;

                  // ── INCOMING ────────────────────────────────────────────────
                  html += '<div style="padding:20px 28px 6px;">';
                  html += _sectionHeader('Phiếu nhập kho từ ĐMH', inCount, '#16a34a');

                  if (inCount > 0) {
                      // Desktop: table
                      html += '<div class="fc-desktop">';
                      html += '<div style="border:1px solid #e5e7eb;border-radius:10px;overflow:hidden;">';
                      html += '<table class="fc-table">';
                      html += '<thead><tr>';
                      html += '<th>Phiếu nhập</th><th>Đơn mua</th><th>Đơn hàng gốc</th><th>Nhà cung cấp</th>';
                      html += '<th>Ngày ĐH MISA</th><th>Ngày hàng về DK</th><th style="text-align:right;">SL nhập</th>';
                      html += '</tr></thead><tbody>';
                      data.incoming.forEach(function(r) {
                          var ss = _stateStyle(r.state);
                          html += '<tr>';
                          html += '<td><span class="fc-pick">'+r.picking_name+'</span><br>';
                          html += '    <span class="fc-badge" style="background:'+ss.bg+';color:'+ss.color+';border-color:'+ss.border+';margin-top:3px;">'+r.state+'</span></td>';
                          html += '<td class="fw-bold" style="color:#2563eb;">'+r.po_name+'</td>';
                          html += '<td style="color:#6b7280;">'+((r.po_origin)||'—')+'</td>';
                          html += '<td>'+r.partner+'</td>';
                          html += '<td>'+r.misa_date+'</td>';
                          html += '<td style="font-weight:700;color:#2563eb;">'+((r.date_planned||r.scheduled_date)||'—')+'</td>';
                          html += '<td style="text-align:right;"><span class="fc-num" style="color:#16a34a;">+'+fmt(r.qty)+'</span></td>';
                          html += '</tr>';
                      });
                      html += '</tbody><tfoot><tr>';
                      html += '<td colspan="6" style="text-align:right;font-weight:700;color:#6b7280;font-size:.78rem;padding:8px 12px;">Tổng nhập:</td>';
                      html += '<td style="text-align:right;"><span class="fc-num" style="color:#16a34a;">+'+fmt(data.total_incoming)+'</span></td>';
                      html += '</tr></tfoot></table></div></div>';

                      // Mobile: cards
                      html += '<div class="fc-mobile">';
                      data.incoming.forEach(function(r) {
                          var ss = _stateStyle(r.state);
                          html += _pickingCard(r.picking_name, r.state, ss, '+'+fmt(r.qty), '#16a34a', '#f0fdf4', [
                              ['Đơn mua',       r.po_name,                        '#2563eb', true],
                              ['Đơn hàng gốc',  r.po_origin,                      '#6b7280', false],
                              ['NCC',           r.partner,                        '#374151', false],
                              ['MISA',          r.misa_date,                      '#374151', false],
                              ['Hàng về',       r.date_planned||r.scheduled_date, '#2563eb', true],
                          ]);
                      });
                      html += '<div style="text-align:right;font-size:.78rem;font-weight:700;color:#16a34a;padding-bottom:10px;">Tổng nhập: +'+fmt(data.total_incoming)+'</div>';
                      html += '</div>';

                  } else {
                      html += _empty('Không có phiếu nhập nào từ đơn mua hàng');
                  }
                  html += '</div>';

                  // ── OUTGOING ────────────────────────────────────────────────
                  html += '<div style="padding:4px 28px 24px;">';
                  html += '<div style="border-top:1px solid #f3f4f6;padding-top:18px;">';
                  html += _sectionHeader('Phiếu xuất kho từ ĐBH', outCount, '#dc2626');

                  if (outCount > 0) {
                      // Desktop: table
                      html += '<div class="fc-desktop">';
                      html += '<div style="border:1px solid #e5e7eb;border-radius:10px;overflow:hidden;">';
                      html += '<table class="fc-table">';
                      html += '<thead><tr>';
                      html += '<th>Phiếu xuất</th><th>Đơn bán</th><th>Khách hàng</th>';
                      html += '<th>Giao hàng DK</th><th style="text-align:right;">SL xuất</th>';
                      html += '</tr></thead><tbody>';
                      data.outgoing.forEach(function(r) {
                          var ss = _stateStyle(r.state);
                          html += '<tr>';
                          html += '<td><span class="fc-pick">'+r.picking_name+'</span><br>';
                          html += '    <span class="fc-badge" style="background:'+ss.bg+';color:'+ss.color+';border-color:'+ss.border+';margin-top:3px;">'+r.state+'</span></td>';
                          html += '<td class="fw-bold" style="color:#2563eb;">'+r.so_name+'</td>';
                          html += '<td>'+r.partner+'</td>';
                          html += '<td style="font-weight:700;color:#2563eb;">'+(r.scheduled_date||'—')+'</td>';
                          html += '<td style="text-align:right;"><span class="fc-num" style="color:#dc2626;">−'+fmt(r.qty)+'</span></td>';
                          html += '</tr>';
                      });
                      html += '</tbody><tfoot><tr>';
                      html += '<td colspan="4" style="text-align:right;font-weight:700;color:#6b7280;font-size:.78rem;padding:8px 12px;">Tổng xuất:</td>';
                      html += '<td style="text-align:right;"><span class="fc-num" style="color:#dc2626;">−'+fmt(data.total_outgoing)+'</span></td>';
                      html += '</tr></tfoot></table></div></div>';

                      // Mobile: cards
                      html += '<div class="fc-mobile">';
                      data.outgoing.forEach(function(r) {
                          var ss = _stateStyle(r.state);
                          html += _pickingCard(r.picking_name, r.state, ss, '−'+fmt(r.qty), '#dc2626', '#fef2f2', [
                              ['Đơn bán',    r.so_name,        '#2563eb', true],
                              ['Khách hàng', r.partner,        '#374151', false],
                              ['Giao hàng',  r.scheduled_date, '#2563eb', true],
                          ]);
                      });
                      html += '<div style="text-align:right;font-size:.78rem;font-weight:700;color:#dc2626;padding-bottom:10px;">Tổng xuất: −'+fmt(data.total_outgoing)+'</div>';
                      html += '</div>';

                  } else {
                      html += _empty('Không có phiếu xuất nào từ đơn bán hàng');
                  }
                  html += '</div></div>';

                  return html;
              }

              function _pill(label, val, bg, border, valColor, labelColor, large, shadow) {
                  var sz = large ? '28px' : '26px';
                  var fw = large ? '900' : '800';
                  return '<div style="text-align:center;background:'+bg+';border:1.5px solid '+border
                      +';border-radius:12px;padding:12px 22px;min-width:90px;box-shadow:0 1px 4px '+(shadow||'rgba(0,0,0,.07)')+';"><div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.8px;color:'+(labelColor||'#9ca3af')+';margin-bottom:4px;">'+label+'</div>'
                      +'<div style="font-size:'+sz+';font-weight:'+fw+';color:'+(valColor||'#111827')+';line-height:1;">'+val+'</div></div>';
              }

              function _sectionHeader(title, count, color) {
                  return '<div style="display:flex;align-items:center;gap:8px;margin-bottom:14px;">'
                      + '<div style="width:4px;height:20px;background:'+color+';border-radius:3px;"></div>'
                      + '<span style="font-size:.95rem;font-weight:700;color:#111827;">'+title+'</span>'
                      + '<span style="background:'+(count>0?color:'#9ca3af')+';color:#fff;border-radius:999px;font-size:.72rem;font-weight:700;padding:2px 9px;">'+count+'</span>'
                      + '</div>';
              }
              function _empty(msg) {
                  return '<div style="background:#f9fafb;border:1.5px dashed #e5e7eb;border-radius:10px;padding:20px;text-align:center;color:#9ca3af;font-size:.85rem;margin-bottom:14px;">'+msg+'</div>';
              }
              function _pickingCard(name, state, ss, qty, qColor, hBg, meta) {
                  var h = '<div style="background:#fff;border:1px solid #e5e7eb;border-left:4px solid '+qColor+';border-radius:10px;margin-bottom:10px;overflow:hidden;">';
                  h += '<div style="display:flex;align-items:center;justify-content:space-between;padding:9px 14px;background:'+hBg+';">';
                  h += '<div style="display:flex;align-items:center;gap:7px;flex-wrap:wrap;">';
                  h += '<span style="font-weight:700;color:#374151;">'+name+'</span>';
                  h += '<span class="fc-badge" style="background:'+ss.bg+';color:'+ss.color+';border-color:'+ss.border+';">'+state+'</span>';
                  h += '</div><span style="font-weight:600;color:'+qColor+';font-size:1rem;">'+qty+'</span></div>';
                  h += '<div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;padding:12px 14px;">';
                  meta.forEach(function(c) {
                      if (!c[1]) return;
                      h += '<div><div style="font-size:.63rem;font-weight:700;text-transform:uppercase;letter-spacing:.5px;color:#9ca3af;margin-bottom:2px;">'+c[0]+'</div>'
                          +'<div style="font-size:.82rem;color:'+(c[2]||'#374151')+';font-weight:'+(c[3]?'700':'500')+';">'+c[1]+'</div></div>';
                  });
                  h += '</div></div>';
                  return h;
              }

            })();
          ]]></script>
        </t>
      </main>
    </t>
  </template>
</odoo>
